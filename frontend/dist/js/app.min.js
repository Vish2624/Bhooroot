const Data = {

  subCategories: {
    'seeds': ['Vegetable Seeds', 'Field Crop Seeds', 'Fruit Seeds', 'Flower Seeds', 'Fodder Seeds'],
    'fertilizer': ['Nitrogenous', 'Phosphatic', 'Potassic', 'Complex NPK', 'Organic & Bio'],
    'chemical': ['Insecticides', 'Fungicides', 'Herbicides', 'Plant Growth Regulators'],
    'machinery': ['Tractors', 'Power Tillers', 'Sprayers', 'Harvesting', 'Implements'],
    'irrigation': ['Drip Systems', 'Sprinkler Systems', 'Pumps', 'Pipes & Fittings'],
    'nutrients': ['Micronutrients', 'Seaweed Extract', 'Humic Acid', 'Amino Acids'],
  },

  categories: [
    { id: 'seeds',      label: 'Seeds',            icon: 'noto:seedling',              count: 1240, image: 'https://images.unsplash.com/photo-1523348837708-15d4a09cfac2?w=600&q=90&fit=crop' },
    { id: 'fertilizer', label: 'Fertilizers',      icon: 'noto:potted-plant',          count: 980,  image: 'https://images.unsplash.com/photo-1464226184884-fa280b87c399?w=600&q=90&fit=crop' },
    { id: 'chemical',   label: 'Chemicals',        icon: 'noto:test-tube',             count: 760,  image: 'https://images.unsplash.com/photo-1628352081506-83c43123a6b9?w=600&q=90&fit=crop' },
    { id: 'machinery',  label: 'Machinery',        icon: 'noto:tractor',               count: 430,  image: 'https://images.unsplash.com/photo-1592982537447-7440770cbfc9?w=600&q=90&fit=crop' },
    { id: 'irrigation', label: 'Irrigation',       icon: 'noto:droplet',               count: 590,  image: 'https://images.unsplash.com/photo-1560493676-04071c5f467b?w=600&q=90&fit=crop' },
    { id: 'nutrients',  label: 'Bio Inputs',       icon: 'noto:herb',                  count: 310,  image: 'https://images.unsplash.com/photo-1500937386664-56d1dfef3854?w=600&q=90&fit=crop' },
    { id: 'organic',    label: 'Organic Farming',  icon: 'noto:sheaf-of-rice',         count: 480,  image: 'https://images.unsplash.com/photo-1586201375761-83865001e31c?w=600&q=90&fit=crop' },
    { id: 'animal',     label: 'Animal Husbandry', icon: 'noto:cow',                   count: 220,  image: 'https://images.unsplash.com/photo-1500595046743-cd271d694e30?w=600&q=90&fit=crop' },
    { id: 'tools',      label: 'Hand Tools',       icon: 'noto:wrench',                count: 350,  image: 'https://images.unsplash.com/photo-1581122584612-713f89daa8eb?w=600&q=90&fit=crop' },
    { id: 'storage',    label: 'Post-Harvest',     icon: 'noto:package',               count: 190,  image: 'https://images.unsplash.com/photo-1625246333195-78d9c38ad449?w=600&q=90&fit=crop' },
  ],

  products: [
    // ── Seeds ────────────────────────────────────────────────────────────────
    {
      id: 1, name: 'Hybrid Tomato Seeds (10g)', price: 299, oldPrice: 349, category: 'seeds',
      vendor: 'Mahyco Seeds', rating: 4.8, badge: 'Best Seller',
      image: 'https://images.unsplash.com/photo-1592482893145-b05987d0de18?w=700&q=90&fit=crop',
      desc: 'High-yield F1 hybrid tomato. 95% germination rate. Suitable for all seasons and open-field cultivation.',
    },
    {
      id: 2, name: 'BT Cotton Seeds (450g)', price: 899, oldPrice: 1099, category: 'seeds',
      vendor: 'Rasi Seeds', rating: 4.6, badge: 'Certified',
      image: 'https://images.unsplash.com/photo-1594911771122-3320491b4904?w=700&q=90&fit=crop',
      desc: 'GEAC-approved Bt cotton variety. Bollworm resistant. Ideal for Kharif season across black-soil regions.',
    },
    {
      id: 3, name: 'Hybrid Paddy Seeds (5kg)', price: 599, oldPrice: 749, category: 'seeds',
      vendor: 'Pioneer Seeds', rating: 4.7, badge: 'New',
      image: 'https://images.unsplash.com/photo-1590479773265-7464e5d48118?w=700&q=90&fit=crop',
      desc: 'Short-duration (110 days) high-yielding hybrid paddy. Ideal for irrigated Kharif planting.',
    },
    {
      id: 4, name: 'Sunflower Seeds (1kg)', price: 349, oldPrice: 399, category: 'seeds',
      vendor: 'Advanta Seeds', rating: 4.5, badge: '',
      image: 'https://images.unsplash.com/photo-1597848212624-a19eb35e2651?w=700&q=90&fit=crop',
      desc: 'High oil-content hybrid sunflower. Downy mildew tolerant. Suitable for Rabi and Kharif planting.',
    },
    {
      id: 5, name: 'Maize Hybrid Seeds (5kg)', price: 699, oldPrice: 849, category: 'seeds',
      vendor: 'Bayer Seeds', rating: 4.8, badge: 'Best Seller',
      image: 'https://images.unsplash.com/photo-1551754655-cd27e38d2076?w=700&q=90&fit=crop',
      desc: 'Stay-green maize hybrid with drought tolerance and 8–10 MT/ha yield potential.',
    },
    {
      id: 6, name: 'Chilli Seeds (10g)', price: 449, oldPrice: 549, category: 'seeds',
      vendor: 'East-West Seeds', rating: 4.6, badge: 'Certified',
      image: 'https://images.unsplash.com/photo-1588252303782-cb80119abd6d?w=700&q=90&fit=crop',
      desc: 'Hot chilli hybrid with long shelf life. Suitable for fresh market and dry chilli production.',
    },

    // ── Fertilizers ──────────────────────────────────────────────────────────
    {
      id: 7, name: 'NPK 19:19:19 (5kg)', price: 1299, oldPrice: 1549, category: 'fertilizer',
      vendor: 'Coromandel Int.', rating: 4.8, badge: 'Best Seller',
      image: 'https://images.unsplash.com/photo-1563514227147-6d2ff66de8c4?w=700&q=90&fit=crop',
      desc: 'Fully water-soluble balanced NPK. Suitable for fertigation and foliar spray on all crops.',
    },
    {
      id: 8, name: 'DAP Fertilizer (50kg)', price: 2499, oldPrice: 2999, category: 'fertilizer',
      vendor: 'IFFCO', rating: 4.7, badge: '',
      image: 'https://images.unsplash.com/photo-1628352081506-83c43123a6b9?w=700&q=90&fit=crop',
      desc: 'Di-ammonium Phosphate — superior source of phosphorus and nitrogen for basal application.',
    },
    {
      id: 9, name: 'Organic Compost (25kg)', price: 799, oldPrice: 999, category: 'fertilizer',
      vendor: 'Biofit Organics', rating: 4.5, badge: 'Organic',
      image: 'https://images.unsplash.com/photo-1500937386664-56d1dfef3854?w=700&q=90&fit=crop',
      desc: 'Farm-grade composted organic matter enriched with beneficial microbes. Improves soil health.',
    },
    {
      id: 10, name: 'Urea 46% N (50kg)', price: 1150, oldPrice: 1350, category: 'fertilizer',
      vendor: 'NFL India', rating: 4.6, badge: '',
      image: 'https://images.unsplash.com/photo-1464226184884-fa280b87c399?w=700&q=90&fit=crop',
      desc: 'BIS-certified granular urea with 46% nitrogen. Best for top-dressing in rice, wheat, and sugarcane.',
    },
    {
      id: 11, name: 'Potassium Humate (1kg)', price: 899, oldPrice: 1099, category: 'fertilizer',
      vendor: 'Agri Gold', rating: 4.4, badge: 'New',
      image: 'https://images.unsplash.com/photo-1557234195-bd9f291f65bc?w=700&q=90&fit=crop',
      desc: 'Soluble potassium humate granules. Improves soil structure, CEC, and nutrient uptake efficiency.',
    },

    // ── Chemicals ────────────────────────────────────────────────────────────
    {
      id: 12, name: 'Cypermethrin 25 EC (250ml)', price: 499, oldPrice: 599, category: 'chemical',
      vendor: 'Bayer CropScience', rating: 4.6, badge: 'CIB&RC',
      image: 'https://images.unsplash.com/photo-1611273426858-450d8e3c9fce?w=700&q=90&fit=crop',
      desc: 'Broad-spectrum pyrethroid insecticide. Controls bollworm, aphids, and whitefly on cotton and vegetables.',
    },
    {
      id: 13, name: 'Imidacloprid 17.8 SL (500ml)', price: 649, oldPrice: 799, category: 'chemical',
      vendor: 'Syngenta India', rating: 4.7, badge: 'CIB&RC',
      image: 'https://images.unsplash.com/photo-1510936111840-65e151ad71bb?w=700&q=90&fit=crop',
      desc: 'Systemic neonicotinoid insecticide. Controls thrips, jassids, and BPH in rice, cotton, and vegetables.',
    },
    {
      id: 14, name: 'Mancozeb 75 WP (500g)', price: 399, oldPrice: 449, category: 'chemical',
      vendor: 'UPL Ltd', rating: 4.5, badge: '',
      image: 'https://images.unsplash.com/photo-1512621776951-a57141f2eefd?w=700&q=90&fit=crop',
      desc: 'Protective fungicide for early and late blight control in potato and tomato. Dithiocarbamate group.',
    },
    {
      id: 15, name: 'Glyphosate 41% SL (1L)', price: 799, category: 'chemical',
      vendor: 'Dhanuka Agritech', rating: 4.4, badge: '',
      image: 'https://images.unsplash.com/photo-1500595046743-cd271d694e30?w=700&q=90&fit=crop',
      desc: 'Non-selective systemic herbicide. Controls annual and perennial weeds before sowing.',
    },
    {
      id: 16, name: 'Chlorpyrifos 20 EC (1L)', price: 549, oldPrice: 649, category: 'chemical',
      vendor: 'Dhanuka Agritech', rating: 4.5, badge: 'CIB&RC',
      image: 'https://images.unsplash.com/photo-1586201375761-83865001e31c?w=700&q=90&fit=crop',
      desc: 'Broad-spectrum organophosphate insecticide. Controls stem borer, cutworm, and termites in soil.',
    },

    // ── Machinery ────────────────────────────────────────────────────────────
    {
      id: 17, name: 'VST Shakti Power Tiller', price: 89999, oldPrice: 99999, category: 'machinery',
      vendor: 'VST Tillers', rating: 4.9, badge: 'Top Pick',
      image: 'https://images.unsplash.com/photo-1592982537447-7440770cbfc9?w=700&q=90&fit=crop',
      desc: '9 HP diesel power tiller. Ideal for puddling, ploughing, and inter-cultivation in paddy fields.',
    },
    {
      id: 18, name: 'Knapsack Sprayer 16L', price: 2499, oldPrice: 2999, category: 'machinery',
      vendor: 'Neptune Sprayers', rating: 4.6, badge: '',
      image: 'https://images.unsplash.com/photo-1564032997182-019688408077?w=700&q=90&fit=crop',
      desc: 'Manual knapsack sprayer with brass pump. Adjustable nozzle from fine mist to direct jet.',
    },
    {
      id: 19, name: 'Rotavator 5ft (Tractor-fit)', price: 34999, oldPrice: 39999, category: 'machinery',
      vendor: 'Shaktiman Agro', rating: 4.7, badge: 'New',
      image: 'https://images.unsplash.com/photo-1416879595882-3373a0480b5b?w=700&q=90&fit=crop',
      desc: 'Heavy-duty rotavator. Fits 35–60 HP tractors. 48 L-type blades for deep soil tillage.',
    },
    {
      id: 20, name: 'Mini Tractor 18 HP', price: 249999, oldPrice: 274999, category: 'machinery',
      vendor: 'Mahindra Agri', rating: 4.8, badge: 'Best Seller',
      image: 'https://images.unsplash.com/photo-1530124566582-a618bc2615dc?w=700&q=90&fit=crop',
      desc: 'Compact 18 HP diesel tractor. Suitable for orchards, poly houses, and small landholdings.',
    },
    {
      id: 21, name: 'Seed Drill Machine (7-row)', price: 18999, category: 'machinery',
      vendor: 'Fieldking India', rating: 4.5, badge: '',
      image: 'https://images.unsplash.com/photo-1625246333195-78d9c38ad449?w=700&q=90&fit=crop',
      desc: '7-row seed-cum-fertilizer drill. Works with 35 HP+ tractors. Adjustable row spacing from 15–25 cm.',
    },

    // ── Irrigation ───────────────────────────────────────────────────────────
    {
      id: 22, name: 'Drip Irrigation Kit (1 Acre)', price: 7499, oldPrice: 8999, category: 'irrigation',
      vendor: 'Netafim India', rating: 4.9, badge: 'Best Seller',
      image: 'https://images.unsplash.com/photo-1560493676-04071c5f467b?w=700&q=90&fit=crop',
      desc: 'Complete drip kit for 1 acre. Includes main line, laterals, inline emitters, filter, and valve unit.',
    },
    {
      id: 23, name: 'Sprinkler System (1 Acre)', price: 4999, oldPrice: 5999, category: 'irrigation',
      vendor: 'Jain Irrigation', rating: 4.7, badge: '',
      image: 'https://images.unsplash.com/photo-1530124566582-a618bc2615dc?w=700&q=90&fit=crop',
      desc: 'Portable sprinkler set for 1-acre coverage. Ideal for wheat, groundnut, and fodder crops.',
    },
    {
      id: 24, name: 'Monoblock Pump 1 HP', price: 8999, oldPrice: 10499, category: 'irrigation',
      vendor: 'Kirloskar Bros', rating: 4.8, badge: 'Certified',
      image: 'https://images.unsplash.com/photo-1500595046743-cd271d694e30?w=700&q=90&fit=crop',
      desc: 'Single-phase 1 HP monoblock pump. Max head 30m. Suitable for open wells and shallow borewells.',
    },
    {
      id: 25, name: 'Submersible Pump 2 HP', price: 14999, oldPrice: 17499, category: 'irrigation',
      vendor: 'CRI Pumps', rating: 4.7, badge: '',
      image: 'https://images.unsplash.com/photo-1464226184884-fa280b87c399?w=700&q=90&fit=crop',
      desc: '2 HP borewell submersible pump. Max depth 60m. BEE 5-star rated for energy efficiency.',
    },
    {
      id: 26, name: 'Rain Gun Sprinkler', price: 3499, oldPrice: 3999, category: 'irrigation',
      vendor: 'Rivulis India', rating: 4.6, badge: 'New',
      image: 'https://images.unsplash.com/photo-1551754655-cd27e38d2076?w=700&q=90&fit=crop',
      desc: 'Heavy-duty rain gun with 360° coverage up to 25m radius. Suitable for sugarcane and fodder.',
    },

    // ── Bio Inputs / Nutrients ────────────────────────────────────────────────
    {
      id: 27, name: 'Azospirillum Bio Fertilizer', price: 299, oldPrice: 349, category: 'nutrients',
      vendor: 'Biomax India', rating: 4.5, badge: 'Organic',
      image: 'https://images.unsplash.com/photo-1500937386664-56d1dfef3854?w=700&q=90&fit=crop',
      desc: 'Nitrogen-fixing biofertilizer for cereals, oilseeds, and vegetables. FSSAI & FCO approved.',
    },
    {
      id: 28, name: 'Rhizobium Culture (250g)', price: 249, category: 'nutrients',
      vendor: 'T Stanes & Co.', rating: 4.4, badge: '',
      image: 'https://images.unsplash.com/photo-1416879595882-3373a0480b5b?w=700&q=90&fit=crop',
      desc: 'Legume-specific Rhizobium. Boosts nodulation and atmospheric nitrogen fixation in pulses and soybean.',
    },
    {
      id: 29, name: 'Seaweed Extract (1L)', price: 899, oldPrice: 1099, category: 'nutrients',
      vendor: 'Kelpak India', rating: 4.7, badge: 'Organic',
      image: 'https://images.unsplash.com/photo-1512621776951-a57141f2eefd?w=700&q=90&fit=crop',
      desc: 'Cold-processed seaweed extract rich in cytokinins and auxins. Improves crop quality, root growth, and yield.',
    },
    {
      id: 30, name: 'Neem Oil Cold-pressed (1L)', price: 599, oldPrice: 699, category: 'nutrients',
      vendor: 'Agri Organics', rating: 4.6, badge: 'Organic',
      image: 'https://images.unsplash.com/photo-1586201375761-83865001e31c?w=700&q=90&fit=crop',
      desc: 'Pure cold-pressed neem oil. Controls fungal diseases, mites, and insects organically. OMRI listed.',
    },
  ],

  vendors: [
    {
      id: 1, name: 'Mahyco Seeds', category: 'Seeds', location: 'Jalna, Maharashtra',
      rating: 4.8, since: 1963, products: 120, verified: true,
      image: 'https://images.unsplash.com/photo-1523348837708-15d4a09cfac2?w=600&q=90&fit=crop',
      desc: "India's leading hybrid seed company. Trusted by 8 million farmers. ICAR-recognized breeding programmes.",
    },
    {
      id: 2, name: 'Coromandel Int.', category: 'Fertilizers', location: 'Hyderabad, Telangana',
      rating: 4.7, since: 1961, products: 85, verified: true,
      image: 'https://images.unsplash.com/photo-1416879595882-3373a0480b5b?w=600&q=90&fit=crop',
      desc: 'Top-3 crop nutrition company in India. Specialising in NPK, SSP, and specialty fertilizers.',
    },
    {
      id: 3, name: 'Bayer CropScience', category: 'Chemicals', location: 'Mumbai, Maharashtra',
      rating: 4.6, since: 1993, products: 200, verified: true,
      image: 'https://images.unsplash.com/photo-1611273426858-450d8e3c9fce?w=600&q=90&fit=crop',
      desc: 'Global leader in crop protection. All formulations CIB&RC registered and quality-audited.',
    },
    {
      id: 4, name: 'Netafim India', category: 'Irrigation', location: 'Ahmedabad, Gujarat',
      rating: 4.9, since: 1965, products: 60, verified: true,
      image: 'https://images.unsplash.com/photo-1560493676-04071c5f467b?w=600&q=90&fit=crop',
      desc: "World's #1 drip irrigation company. Pioneer of precision irrigation technology in India.",
    },
    {
      id: 5, name: 'VST Tillers Tractors', category: 'Machinery', location: 'Bengaluru, Karnataka',
      rating: 4.8, since: 1967, products: 40, verified: true,
      image: 'https://images.unsplash.com/photo-1592982537447-7440770cbfc9?w=600&q=90&fit=crop',
      desc: "India's largest power tiller manufacturer. ISO 9001:2015 certified. 5 lakh+ units sold.",
    },
    {
      id: 6, name: 'Jain Irrigation', category: 'Irrigation', location: 'Jalgaon, Maharashtra',
      rating: 4.7, since: 1963, products: 90, verified: true,
      image: 'https://images.unsplash.com/photo-1530124566582-a618bc2615dc?w=600&q=90&fit=crop',
      desc: "World's 2nd largest drip irrigation manufacturer. Diversified agribusiness with 10,000+ SKUs.",
    },
    {
      id: 7, name: 'IFFCO', category: 'Fertilizers', location: 'New Delhi, Delhi',
      rating: 4.5, since: 1967, products: 30, verified: true,
      image: 'https://images.unsplash.com/photo-1464226184884-fa280b87c399?w=600&q=90&fit=crop',
      desc: "India's largest fertilizer cooperative. Serving 55 million+ farmer members across 28 states.",
    },
    {
      id: 8, name: 'Syngenta India', category: 'Chemicals', location: 'Pune, Maharashtra',
      rating: 4.6, since: 1990, products: 150, verified: true,
      image: 'https://images.unsplash.com/photo-1597848212624-a19eb35e2651?w=600&q=90&fit=crop',
      desc: 'Innovative agro solutions for pest, disease, and weed management across major Indian crops.',
    },
  ],

  faqs: [
    {
      question: 'What payment methods are accepted?',
      answer: 'We accept all major payment methods via Razorpay — UPI (GPay, PhonePe, Paytm), Credit/Debit Cards (Visa, Mastercard, RuPay), Net Banking, and Cash on Delivery (COD) for orders under ₹10,000.',
    },
    {
      question: 'How long does delivery take?',
      answer: 'Standard delivery takes 3–7 business days across India. Orders placed before 2 PM are dispatched the same day. Bio-inputs and perishable seeds are shipped via cold-chain logistics to preserve viability.',
    },
    {
      question: 'Do you offer bulk or wholesale discounts?',
      answer: 'Yes! FPOs, cooperative societies, and agro dealers get custom bulk pricing. Submit a Bulk Quote request on the Contact page — our team responds within 24 hours with a tailored offer.',
    },
    {
      question: 'Are all products certified and genuine?',
      answer: 'Absolutely. Every product is sourced directly from licensed manufacturers registered with CIB&RC (chemicals), ICAR (seeds), and FCO (fertilizers). We audit all vendor licences annually and reject non-compliant stock.',
    },
    {
      question: 'What is the return and refund policy?',
      answer: 'Quality issues can be raised within 7 days of delivery. Machinery defects are covered under the manufacturer warranty. Contact support with photos and batch numbers — refunds are processed within 5–7 working days.',
    },
    {
      question: 'How do I track my order?',
      answer: 'Once dispatched, you receive an SMS and email with a live tracking link. You can also enter your Order ID on the Help page below for real-time status. Our support team is available Mon–Sat if you need help.',
    },
    {
      question: 'Do you deliver to rural and remote areas?',
      answer: 'Yes. We deliver to 28 states including rural pincodes via Delhivery, DTDC, and India Post for remote areas. Enter your pincode at checkout to confirm serviceability before placing your order.',
    },
    {
      question: 'Is there a minimum order value?',
      answer: 'No minimum order value for standard products. Free shipping applies on orders above ₹2,000. Bulk orders above ₹50,000 receive dedicated logistics support and a relationship manager.',
    },
    {
      question: 'Do you provide agro advisory services?',
      answer: 'Yes. Certified agronomists are available Mon–Sat, 8 AM – 7 PM on our helpline (+91 98765 43210). Free video consultation is provided with orders above ₹5,000. Advisory is available in Hindi, Tamil, Telugu, Kannada, and Marathi.',
    },
    {
      question: 'How can I become a vendor or reseller partner?',
      answer: 'Visit the Contact page and select "Vendor Partnership" as the subject. Our partner team reviews your licences (seed/fertilizer/pesticide dealer licence) and onboards you within 5–7 working days.',
    },
  ],

  crops: [
    { id: 'paddy',     label: 'Paddy',     icon: 'noto:sheaf-of-rice', category: 'seeds' },
    { id: 'maize',     label: 'Maize',     icon: 'noto:ear-of-corn',   category: 'seeds' },
    { id: 'tomato',    label: 'Tomato',    icon: 'noto:tomato',        category: 'seeds' },
    { id: 'onion',     label: 'Onion',     icon: 'noto:onion',         category: 'seeds' },
    { id: 'chilli',    label: 'Chilli',    icon: 'noto:hot-pepper',    category: 'seeds' },
    { id: 'groundnut', label: 'Groundnut', icon: 'noto:peanuts',       category: 'seeds' },
    { id: 'cotton',    label: 'Cotton',    icon: 'noto:seedling',      category: 'seeds' },
  ],

  ticker: [
    { icon: 'ph:truck-fill',   text: 'Free shipping on orders above ₹2,000' },
    { icon: 'ph:leaf-fill',    text: 'Monsoon Season Sale — Up to 30% off Seeds' },
    { icon: 'noto:tractor',    text: 'New Stock: VST Power Tillers now available' },
    { icon: 'ph:drop-fill',    text: 'Drip Irrigation Kits — Limited stock, order now' },
    { icon: 'ph:bag-fill',     text: 'IFFCO DAP now in stock — buy direct' },
    { icon: 'ph:star-fill',    text: '12,000+ farmers trust Uhazvumart pan-India' },
    { icon: 'ph:flask-fill',   text: 'CIB&RC certified chemicals — genuine stock only' },
    { icon: 'ph:package-fill', text: 'Bulk order? Custom quote within 24 hrs' },
  ],

  sliderImages: [
    {
      image: 'https://images.unsplash.com/photo-1574943320219-553eb213f72d?w=1400&q=90&fit=crop&crop=center',
      label: 'Premium Hybrid Seeds',
      sub: '95% germination rate · ICAR-tested varieties',
    },
    {
      image: 'https://images.unsplash.com/photo-1500937386664-56d1dfef3854?w=1400&q=90&fit=crop&crop=center',
      label: 'Certified Fertilizers',
      sub: 'FCO-approved · direct from manufacturers',
    },
    {
      image: 'https://images.unsplash.com/photo-1592982537447-7440770cbfc9?w=1400&q=90&fit=crop&crop=center',
      label: 'Farm Machinery',
      sub: 'Tractors, tillers & tools — delivered to your farm',
    },
    {
      image: 'https://images.unsplash.com/photo-1560493676-04071c5f467b?w=1400&q=90&fit=crop&crop=center',
      label: 'Drip Irrigation Systems',
      sub: 'Save 50% water · grow more with precision',
    },
  ],
};

// ============================================================
// api.js — Central API configuration & helpers
// ============================================================

const Api = {
  // Use localhost if opened directly via file://, otherwise use the current origin
  BASE_URL: (window.location.protocol === 'file:' || window.location.origin === 'null' || window.location.origin === 'file://') 
    ? 'http://localhost:5000' 
    : window.location.origin,

  // Google Sheets auth — used when no backend is available (GitHub Pages)
  _SHEETS_URL: 'https://script.google.com/macros/s/AKfycbxVfqkoILhHmuQqx-Lr5DMS17QHfJHiwqTak1uE6uxY8jY7W5qEU0dCW_bXArxQIYyJ/exec',
  _isGitHubPages() {
    return window.location.hostname.includes('github.io');
  },
  async _sheetsCall(action, payload) {
    const url = new URL(this._SHEETS_URL);
    url.searchParams.set('action', action);
    Object.entries(payload).forEach(([k, v]) => v && url.searchParams.set(k, v));
    const res = await fetch(url.toString());
    const data = await res.json();
    if (!data.success) throw new Error(data.message || 'Auth failed');
    return data;
  },

  // ── Token helpers (localStorage) ────────────────────────
  getToken() {
    return localStorage.getItem('um_token') || null;
  },
  setToken(token) {
    localStorage.setItem('um_token', token);
  },
  clearToken() {
    localStorage.removeItem('um_token');
    localStorage.removeItem('um_user');
  },

  getUser() {
    try { return JSON.parse(localStorage.getItem('um_user')); } catch { return null; }
  },
  setUser(user) {
    localStorage.setItem('um_user', JSON.stringify(user));
  },

  // ── Base fetch with JSON + auth header ──────────────────
  async request(path, options = {}) {
    const token = this.getToken();
    const headers = { 'Content-Type': 'application/json', ...(options.headers || {}) };
    if (token) headers['Authorization'] = `Bearer ${token}`;

    const res = await fetch(`${this.BASE_URL}${path}`, { ...options, headers });
    const data = await res.json().catch(() => ({}));
    if (!res.ok) throw Object.assign(new Error(data.message || 'API error'), { status: res.status, data });
    return data;
  },

  // ── Health ───────────────────────────────────────────────
  health() {
    return this.request('/api/health');
  },

  // ── Products ─────────────────────────────────────────────
  getProducts(params = {}) {
    const qs = new URLSearchParams(params).toString();
    return this.request(`/api/products${qs ? '?' + qs : ''}`);
  },
  getProduct(id) {
    return this.request(`/api/products/${id}`);
  },

  // ── Vendors ──────────────────────────────────────────────
  getVendors() {
    return this.request('/api/vendors');
  },

  // ── Auth ─────────────────────────────────────────────────
  async register(name, email, phone, password) {
    if (this._isGitHubPages()) {
      const data = await this._sheetsCall('register', { name, email, phone, password });
      this.setUser(data.user);
      return data;
    }
    const data = await this.request('/api/auth/register', {
      method: 'POST',
      body: JSON.stringify({ name, email, phone, password }),
    });
    if (data.token) { this.setToken(data.token); this.setUser(data.user); }
    return data;
  },
  async login(email, password) {
    if (this._isGitHubPages()) {
      const data = await this._sheetsCall('login', { email, password });
      this.setUser(data.user);
      return data;
    }
    const data = await this.request('/api/auth/login', {
      method: 'POST',
      body: JSON.stringify({ email, password }),
    });
    if (data.token) { this.setToken(data.token); this.setUser(data.user); }
    return data;
  },
  logout() {
    this.clearToken();
  },

  // ── Payment ──────────────────────────────────────────────
  createPayment(items, total) {
    return this.request('/api/payment', {
      method: 'POST',
      body: JSON.stringify({ items, total }),
    });
  },

  // ── Orders ───────────────────────────────────────────────
  createOrder(payload) {
    return this.request('/api/orders', {
      method: 'POST',
      body: JSON.stringify(payload),
    });
  },
  getOrder(id) {
    return this.request(`/api/orders/${id}`);
  },

  // ── Public — Banners & CMS (storefront reads) ────────────
  getBanners(type) {
    const qs = type ? `?type=${encodeURIComponent(type)}` : '';
    return this.request(`/api/banners${qs}`);
  },
  getCmsSection(key) {
    return this.request(`/api/cms/${encodeURIComponent(key)}`);
  },
};

/* ═══════════════════════════════════════════
   LANG — language selector, detection, i18n
═══════════════════════════════════════════ */

const Lang = {

  LANGUAGES: [
    { code: 'en', label: 'English',   native: 'English',    bcp: 'en' },
    { code: 'hi', label: 'Hindi',     native: 'हिन्दी',      bcp: 'hi' },
    { code: 'ta', label: 'Tamil',     native: 'தமிழ்',      bcp: 'ta' },
    { code: 'te', label: 'Telugu',    native: 'తెలుగు',     bcp: 'te' },
    { code: 'kn', label: 'Kannada',   native: 'ಕನ್ನಡ',    bcp: 'kn' },
    { code: 'ml', label: 'Malayalam', native: 'മലയാളം',  bcp: 'ml' },
    { code: 'bn', label: 'Bengali',   native: 'বাংলা',    bcp: 'bn' },
    { code: 'mr', label: 'Marathi',   native: 'मराठी',    bcp: 'mr' },
    { code: 'gu', label: 'Gujarati',  native: 'ગુજરાતી',   bcp: 'gu' },
    { code: 'pa', label: 'Punjabi',   native: 'ਪੰਜਾਬੀ',    bcp: 'pa' },
    { code: 'or', label: 'Odia',      native: 'ଓଡ଼ିଆ',      bcp: 'or' },
  ],

  STRINGS: {
    en: {
      home: 'Home', products: 'Products', vendors: 'Vendors', contact: 'Contact',
      getQuote: 'Get Quote', login: 'Login',
      searchPlaceholder: 'Search for seeds, fertilizers, tractors, pesticides…',
      language: 'Language',
      heroPill: 'Trusted by 12,000+ Farmers Across India',
      heroTitle: 'Buy Genuine<br/>Agricultural Products<br/><em>Direct From</em> <strong>Trusted Brands</strong>',
      heroSubtitle: 'Quality agro products delivered direct to your farm gate',
      heroCheck1: 'Certified Products', heroCheck2: 'Cash on Delivery',
      heroCheck3: 'Bulk Orders Available', heroCheck4: 'Delivery Across India',
      shopProducts: 'Shop Products', getBulkQuote: 'Get Bulk Quote',
      statProducts: 'Products', statBrands: 'Brands', statStates: 'States', statFarmers: 'Farmers',
      trust1Title: '100% Genuine Products', trust1Desc: 'CIB&RC registered manufacturers',
      trust2Title: 'Cash on Delivery', trust2Desc: 'COD available up to ₹10,000',
      trust3Title: 'Secure Payments', trust3Desc: 'UPI, Cards & Net Banking',
      trust4Title: 'Expert Farming Support', trust4Desc: 'Agronomists Mon–Sat, 8 AM–7 PM',
      bestSellersEyebrow: 'Best Sellers', bestSellersTitle: 'Top Selling Products', viewAll: 'View All →',
      catEyebrow: 'Shop By Category', catTitle: 'Everything Your Farm Needs',
      catSub: '5,000+ certified products across 10 major agro input categories',
      productsTitle: 'Our Products', productsSubtitle: '5,000+ certified agro inputs — seeds, fertilizers, chemicals, machinery & irrigation',
      categoryLabel: 'Category', filterAll: 'All Products', filterSeeds: 'Seeds', filterFertilizer: 'Fertilizers',
      filterChemicals: 'Chemicals', filterMachinery: 'Machinery', filterIrrigation: 'Irrigation', filterBioInputs: 'Bio Inputs',
      vendorsEyebrow: 'Authorised Partners', vendorsTitle: 'Our Vendor Partners', vendorsSubtitle: 'All brands are KYC-verified, licence-checked, and quality-audited annually by our team.',
      contactEyebrow: 'Get in Touch', contactTitle: "We're Here to Help", contactSubtitle: 'Have questions about products, bulk orders, or delivery? Our certified agro experts are ready to assist Mon–Sat.',
      helpEyebrow: 'Support Centre', helpTitle: 'Help & FAQ',
    },
    hi: {
      home: 'होम', products: 'उत्पाद', vendors: 'विक्रेता', contact: 'संपर्क',
      getQuote: 'कोटेशन पाएं', login: 'लॉगिन',
      searchPlaceholder: 'बीज, उर्वरक, ट्रैक्टर, कीटनाशकों की खोज करें…',
      language: 'भाषा',
      heroPill: '12,000+ किसानों द्वारा विश्वसनीय',
      heroTitle: 'असली<br/>कृषि उत्पाद खरीदें<br/><em>सीधे</em> <strong>विश्वसनीय ब्रांड्स से</strong>',
      heroSubtitle: 'गुणवत्तापूर्ण कृषि उत्पाद सीधे आपके खेत तक',
      heroCheck1: 'प्रमाणित उत्पाद', heroCheck2: 'कैश ऑन डिलीवरी',
      heroCheck3: 'बल्क ऑर्डर उपलब्ध', heroCheck4: 'पूरे भारत में डिलीवरी',
      shopProducts: 'उत्पाद देखें', getBulkQuote: 'बल्क कोटेशन पाएं',
      statProducts: 'उत्पाद', statBrands: 'ब्रांड्स', statStates: 'राज्य', statFarmers: 'किसान',
      trust1Title: '100% असली उत्पाद', trust1Desc: 'CIB&RC पंजीकृत निर्माता',
      trust2Title: 'कैश ऑन डिलीवरी', trust2Desc: '₹10,000 तक COD उपलब्ध',
      trust3Title: 'सुरक्षित भुगतान', trust3Desc: 'UPI, कार्ड और नेट बैंकिंग',
      trust4Title: 'विशेषज्ञ कृषि सहायता', trust4Desc: 'कृषि विशेषज्ञ सोम–शनि, सुबह 8–शाम 7',
      bestSellersEyebrow: 'सर्वश्रेष्ठ विक्रेता', bestSellersTitle: 'सबसे ज़्यादा बिकने वाले उत्पाद', viewAll: 'सभी देखें →',
      catEyebrow: 'श्रेणी से खरीदें', catTitle: 'आपके खेत की हर ज़रूरत',
      catSub: '10 प्रमुख कृषि श्रेणियों में 5,000+ प्रमाणित उत्पाद',
      productsTitle: 'हमारे उत्पाद', productsSubtitle: '5,000+ प्रमाणित कृषि उत्पाद — बीज, उर्वरक, रसायन, मशीनरी और सिंचाई',
      categoryLabel: 'श्रेणी', filterAll: 'सभी उत्पाद', filterSeeds: 'बीज', filterFertilizer: 'उर्वरक',
      filterChemicals: 'रसायन', filterMachinery: 'मशीनरी', filterIrrigation: 'सिंचाई', filterBioInputs: 'जैव इनपुट',
      vendorsEyebrow: 'अधिकृत भागीदार', vendorsTitle: 'हमारे विक्रेता भागीदार', vendorsSubtitle: 'सभी ब्रांड KYC-सत्यापित, लाइसेंस-जांच किए गए और वार्षिक गुणवत्ता-ऑडिट हैं।',
      contactEyebrow: 'संपर्क करें', contactTitle: 'हम मदद के लिए यहाँ हैं', contactSubtitle: 'उत्पादों, बल्क ऑर्डर या डिलीवरी के बारे में प्रश्न हैं? हमारे विशेषज्ञ सोम–शनि उपलब्ध हैं।',
      helpEyebrow: 'सहायता केंद्र', helpTitle: 'मदद और FAQ',
    },
    ta: {
      home: 'முகப்பு', products: 'தயாரிப்புகள்', vendors: 'விற்பனையாளர்கள்', contact: 'தொடர்பு',
      getQuote: 'மேற்கோள் பெறுக', login: 'உள்நுழைய',
      searchPlaceholder: 'விதைகள், உரங்கள், டிராக்டர்கள், பூச்சிக்கொல்லிகளைத் தேடுங்கள்…',
      language: 'மொழி',
      heroPill: '12,000+ விவசாயிகளால் நம்பப்படுகிறது',
      heroTitle: 'உண்மையான<br/>விவசாய தயாரிப்புகள் வாங்குங்கள்<br/><em>நேரடியாக</em> <strong>நம்பகமான பிராண்டுகளிலிருந்து</strong>',
      heroSubtitle: 'தரமான விவசாய பொருட்கள் உங்கள் வயலுக்கே',
      heroCheck1: 'சான்றளிக்கப்பட்ட தயாரிப்புகள்', heroCheck2: 'விநியோகத்தில் பணம்',
      heroCheck3: 'மொத்த ஆர்டர்கள் கிடைக்கும்', heroCheck4: 'இந்தியா முழுவதும் விநியோகம்',
      shopProducts: 'தயாரிப்புகளை வாங்கு', getBulkQuote: 'மொத்த மேற்கோள் பெறு',
      statProducts: 'தயாரிப்புகள்', statBrands: 'பிராண்டுகள்', statStates: 'மாநிலங்கள்', statFarmers: 'விவசாயிகள்',
      trust1Title: '100% உண்மையான பொருட்கள்', trust1Desc: 'CIB&RC பதிவு செய்யப்பட்ட உற்பத்தியாளர்கள்',
      trust2Title: 'விநியோகத்தில் பணம்', trust2Desc: '₹10,000 வரை COD கிடைக்கும்',
      trust3Title: 'பாதுகாப்பான கட்டணம்', trust3Desc: 'UPI, அட்டைகள் மற்றும் நெட் வங்கி',
      trust4Title: 'நிபுணர் விவசாய ஆதரவு', trust4Desc: 'வேளாண் நிபுணர்கள் திங்கள்–சனி, காலை 8–மாலை 7',
      bestSellersEyebrow: 'சிறந்த விற்பனையாளர்கள்', bestSellersTitle: 'அதிகம் விற்கப்படும் தயாரிப்புகள்', viewAll: 'அனைத்தும் காண்க →',
      catEyebrow: 'வகையின்படி வாங்கு', catTitle: 'உங்கள் நிலத்திற்கு தேவையான அனைத்தும்',
      catSub: '10 முக்கிய வகைகளில் 5,000+ சான்றளிக்கப்பட்ட தயாரிப்புகள்',
      productsTitle: 'எங்கள் தயாரிப்புகள்', productsSubtitle: '5,000+ சான்றளிக்கப்பட்ட விவசாய பொருட்கள் — விதைகள், உரங்கள், இரசாயனங்கள், இயந்திரங்கள் மற்றும் நீர்ப்பாசனம்',
      categoryLabel: 'வகை', filterAll: 'அனைத்து தயாரிப்புகள்', filterSeeds: 'விதைகள்', filterFertilizer: 'உரங்கள்',
      filterChemicals: 'இரசாயனங்கள்', filterMachinery: 'இயந்திரங்கள்', filterIrrigation: 'நீர்ப்பாசனம்', filterBioInputs: 'உயிரி உள்ளீடுகள்',
      vendorsEyebrow: 'அங்கீகரிக்கப்பட்ட கூட்டாளர்கள்', vendorsTitle: 'எங்கள் விற்பனையாளர் கூட்டாளர்கள்', vendorsSubtitle: 'அனைத்து பிராண்டுகளும் KYC சரிபார்க்கப்பட்டவை, உரிமம் சரிபார்க்கப்பட்டவை மற்றும் ஆண்டுதோறும் தர தணிக்கை செய்யப்படுகின்றன.',
      contactEyebrow: 'தொடர்பு கொள்ளுங்கள்', contactTitle: 'நாங்கள் உதவ இங்கே இருக்கிறோம்', contactSubtitle: 'தயாரிப்புகள், மொத்த ஆர்டர்கள் அல்லது விநியோகம் பற்றி கேள்விகளா? எங்கள் நிபுணர்கள் திங்கள்–சனி கிடைக்கின்றனர்.',
      helpEyebrow: 'ஆதரவு மையம்', helpTitle: 'உதவி மற்றும் FAQ',
    },
    te: {
      home: 'హోమ్', products: 'ఉత్పత్తులు', vendors: 'విక్రేతలు', contact: 'సంప్రదింపు',
      getQuote: 'కోటేషన్ పొందండి', login: 'లాగిన్',
      searchPlaceholder: 'విత్తనాలు, ఎరువులు, ట్రాక్టర్లు, పురుగుమందులు వెతకండి…',
      language: 'భాష',
      heroPill: '12,000+ రైతులచే విశ్వసించబడింది',
      heroTitle: 'నిజమైన<br/>వ్యవసాయ ఉత్పత్తులు కొనండి<br/><em>నేరుగా</em> <strong>విశ్వసనీయ బ్రాండ్లనుండి</strong>',
      heroSubtitle: 'నాణ్యమైన వ్యవసాయ ఉత్పత్తులు మీ పొలానికే',
      heroCheck1: 'ధృవీకరించబడిన ఉత్పత్తులు', heroCheck2: 'కేష్ ఆన్ డెలివరీ',
      heroCheck3: 'బల్క్ ఆర్డర్లు అందుబాటులో', heroCheck4: 'భారతదేశమంతటా డెలివరీ',
      shopProducts: 'ఉత్పత్తులు కొనండి', getBulkQuote: 'బల్క్ కోటేషన్ పొందండి',
      statProducts: 'ఉత్పత్తులు', statBrands: 'బ్రాండ్లు', statStates: 'రాష్ట్రాలు', statFarmers: 'రైతులు',
      trust1Title: '100% నిజమైన ఉత్పత్తులు', trust1Desc: 'CIB&RC నమోదిత తయారీదారులు',
      trust2Title: 'కేష్ ఆన్ డెలివరీ', trust2Desc: '₹10,000 వరకు COD అందుబాటులో',
      trust3Title: 'సురక్షిత చెల్లింపులు', trust3Desc: 'UPI, కార్డులు మరియు నెట్ బ్యాంకింగ్',
      trust4Title: 'నిపుణుల వ్యవసాయ సహాయం', trust4Desc: 'వ్యవసాయ నిపుణులు సోమ–శని, ఉ 8–సా 7',
      bestSellersEyebrow: 'బెస్ట్ సెల్లర్లు', bestSellersTitle: 'అత్యధికంగా అమ్ముడైన ఉత్పత్తులు', viewAll: 'అన్నీ చూడండి →',
      catEyebrow: 'వర్గం ప్రకారం కొనండి', catTitle: 'మీ పొలానికి అవసరమైన అన్నీ',
      catSub: '10 ప్రధాన వర్గాలలో 5,000+ ధృవీకరించబడిన ఉత్పత్తులు',
      productsTitle: 'మా ఉత్పత్తులు', productsSubtitle: '5,000+ ధృవీకరించిన వ్యవసాయ ఉత్పత్తులు — విత్తనాలు, ఎరువులు, రసాయనాలు, యంత్రాలు మరియు నీటిపారుదల',
      categoryLabel: 'వర్గం', filterAll: 'అన్ని ఉత్పత్తులు', filterSeeds: 'విత్తనాలు', filterFertilizer: 'ఎరువులు',
      filterChemicals: 'రసాయనాలు', filterMachinery: 'యంత్రాలు', filterIrrigation: 'నీటిపారుదల', filterBioInputs: 'జైవ ఇన్‌పుట్‌లు',
      vendorsEyebrow: 'అధికారిక భాగస్వాములు', vendorsTitle: 'మా విక్రేత భాగస్వాములు', vendorsSubtitle: 'అన్ని బ్రాండ్లు KYC ధృవీకరించబడ్డాయి మరియు వార్షికంగా నాణ్యత ఆడిట్ చేయబడతాయి.',
      contactEyebrow: 'సంప్రదించండి', contactTitle: 'మేము సహాయం చేయడానికి ఇక్కడ ఉన్నాం', contactSubtitle: 'ఉత్పత్తులు, బల్క్ ఆర్డర్లు లేదా డెలివరీ గురించి ప్రశ్నలు ఉన్నాయా? మా నిపుణులు సోమ–శని అందుబాటులో ఉంటారు.',
      helpEyebrow: 'మద్దతు కేంద్రం', helpTitle: 'సహాయం & FAQ',
    },
    kn: {
      home: 'ಮುಖಪುಟ', products: 'ಉತ್ಪನ್ನಗಳು', vendors: 'ಮಾರಾಟಗಾರರು', contact: 'ಸಂಪರ್ಕ',
      getQuote: 'ಕೋಟ್ ಪಡೆಯಿರಿ', login: 'ಲಾಗಿನ್',
      searchPlaceholder: 'ಬೀಜಗಳು, ಗೊಬ್ಬರಗಳು, ಟ್ರ್ಯಾಕ್ಟರ್, ಕೀಟನಾಶಕ ಹುಡುಕಿ…',
      language: 'ಭಾಷೆ',
      heroPill: '12,000+ ರೈತರ ವಿಶ್ವಾಸ',
      heroTitle: 'ಅಸಲಿ<br/>ಕೃಷಿ ಉತ್ಪನ್ನಗಳನ್ನು ಖರೀದಿಸಿ<br/><em>ನೇರವಾಗಿ</em> <strong>ವಿಶ್ವಸನೀಯ ಬ್ರ್ಯಾಂಡ್‌ಗಳಿಂದ</strong>',
      heroSubtitle: 'ಗುಣಮಟ್ಟದ ಕೃಷಿ ಉತ್ಪನ್ನಗಳು ನಿಮ್ಮ ಹೊಲಕ್ಕೇ',
      heroCheck1: 'ಪ್ರಮಾಣೀಕೃತ ಉತ್ಪನ್ನಗಳು', heroCheck2: 'ಕ್ಯಾಶ್ ಆನ್ ಡೆಲಿವರಿ',
      heroCheck3: 'ಬಲ್ಕ್ ಆರ್ಡರ್ ಲಭ್ಯ', heroCheck4: 'ಭಾರತದ ತುಂಬ ಡೆಲಿವರಿ',
      shopProducts: 'ಉತ್ಪನ್ನಗಳನ್ನು ಖರೀದಿಸಿ', getBulkQuote: 'ಬಲ್ಕ್ ಕೋಟ್ ಪಡೆಯಿರಿ',
      statProducts: 'ಉತ್ಪನ್ನಗಳು', statBrands: 'ಬ್ರ್ಯಾಂಡ್‌ಗಳು', statStates: 'ರಾಜ್ಯಗಳು', statFarmers: 'ರೈತರು',
      trust1Title: '100% ಅಸಲಿ ಉತ್ಪನ್ನಗಳು', trust1Desc: 'CIB&RC ನೋಂದಾಯಿತ ತಯಾರಕರು',
      trust2Title: 'ಕ್ಯಾಶ್ ಆನ್ ಡೆಲಿವರಿ', trust2Desc: '₹10,000 ವರೆಗೆ COD ಲಭ್ಯ',
      trust3Title: 'ಸುರಕ್ಷಿತ ಪಾವತಿ', trust3Desc: 'UPI, ಕಾರ್ಡ್ ಮತ್ತು ನೆಟ್ ಬ್ಯಾಂಕಿಂಗ್',
      trust4Title: 'ತಜ್ಞ ಕೃಷಿ ಬೆಂಬಲ', trust4Desc: 'ಕೃಷಿ ತಜ್ಞರು ಸೋಮ–ಶನಿ, ಬೆ 8–ಸಂ 7',
      bestSellersEyebrow: 'ಬೆಸ್ಟ್ ಸೆಲ್ಲರ್ಸ್', bestSellersTitle: 'ಅತ್ಯಧಿಕ ಮಾರಾಟದ ಉತ್ಪನ್ನಗಳು', viewAll: 'ಎಲ್ಲ ನೋಡಿ →',
      catEyebrow: 'ವರ್ಗದ ಪ್ರಕಾರ ಖರೀದಿಸಿ', catTitle: 'ನಿಮ್ಮ ಹೊಲಕ್ಕೆ ಬೇಕಾದ ಎಲ್ಲವೂ',
      catSub: '10 ಪ್ರಮುಖ ವರ್ಗಗಳಲ್ಲಿ 5,000+ ಪ್ರಮಾಣೀಕೃತ ಉತ್ಪನ್ನಗಳು',
      productsTitle: 'ನಮ್ಮ ಉತ್ಪನ್ನಗಳು', productsSubtitle: '5,000+ ಪ್ರಮಾಣೀಕೃತ ಕೃಷಿ ಉತ್ಪನ್ನಗಳು — ಬೀಜಗಳು, ಗೊಬ್ಬರಗಳು, ರಾಸಾಯನಿಕಗಳು, ಯಂತ್ರಗಳು ಮತ್ತು ನೀರಾವರಿ',
      categoryLabel: 'ವರ್ಗ', filterAll: 'ಎಲ್ಲ ಉತ್ಪನ್ನಗಳು', filterSeeds: 'ಬೀಜಗಳು', filterFertilizer: 'ಗೊಬ್ಬರಗಳು',
      filterChemicals: 'ರಾಸಾಯನಿಕಗಳು', filterMachinery: 'ಯಂತ್ರಗಳು', filterIrrigation: 'ನೀರಾವರಿ', filterBioInputs: 'ಜೈವಿಕ ಇನ್‌ಪುಟ್‌ಗಳು',
      vendorsEyebrow: 'ಅಧಿಕೃತ ಪಾಲುದಾರರು', vendorsTitle: 'ನಮ್ಮ ವಿಕ್ರೇತ ಪಾಲುದಾರರು', vendorsSubtitle: 'ಎಲ್ಲ ಬ್ರಾಂಡ್‌ಗಳು KYC-ಪರಿಶೀಲಿತ ಮತ್ತು ವಾರ್ಷಿಕ ಗುಣಮಟ್ಟ ಆಡಿಟ್ ಮಾಡಲ್ಪಟ್ಟಿವೆ.',
      contactEyebrow: 'ಸಂಪರ್ಕಿಸಿ', contactTitle: 'ನಾವು ಸಹಾಯ ಮಾಡಲು ಇಲ್ಲಿದ್ದೇವೆ', contactSubtitle: 'ಉತ್ಪನ್ನಗಳು, ಬಲ್ಕ್ ಆರ್ಡರ್ ಅಥವಾ ಡೆಲಿವರಿ ಬಗ್ಗೆ ಪ್ರಶ್ನೆಗಳಿವೆಯೇ? ನಮ್ಮ ತಜ್ಞರು ಸೋಮ–ಶನಿ ಲಭ್ಯ.',
      helpEyebrow: 'ಬೆಂಬಲ ಕೇಂದ್ರ', helpTitle: 'ಸಹಾಯ & FAQ',
    },
    ml: {
      home: 'ഹോം', products: 'ഉൽപ്പന്നങ്ങൾ', vendors: 'വിൽപ്പനക്കാർ', contact: 'ബന്ധപ്പെടുക',
      getQuote: 'ക്വോട്ട് നേടുക', login: 'ലോഗിൻ',
      searchPlaceholder: 'വിത്തുകൾ, വളങ്ങൾ, ട്രാക്ടറുകൾ, കീടനാശിനികൾ തിരയൂ…',
      language: 'ഭാഷ',
      heroPill: '12,000+ കർഷകർ വിശ്വസിക്കുന്നു',
      heroTitle: 'യഥാർഥ<br/>കൃഷി ഉൽപ്പന്നങ്ങൾ വാങ്ങൂ<br/><em>നേരിട്ട്</em> <strong>വിശ്വസ്ത ബ്രാൻഡുകളിൽ നിന്ന്</strong>',
      heroSubtitle: 'ഗുണമേന്മയുള്ള കൃഷി ഉൽപ്പന്നങ്ങൾ നിങ്ങളുടെ വയലിലേക്ക്',
      heroCheck1: 'സർട്ടിഫൈഡ് ഉൽപ്പന്നങ്ങൾ', heroCheck2: 'കാഷ് ഓൺ ഡെലിവറി',
      heroCheck3: 'ബൾക്ക് ഓർഡർ ലഭ്യം', heroCheck4: 'ഇന്ത്യ മുഴുവൻ ഡെലിവറി',
      shopProducts: 'ഉൽപ്പന്നങ്ങൾ വാങ്ങൂ', getBulkQuote: 'ബൾക്ക് ക്വോട്ട് നേടൂ',
      statProducts: 'ഉൽപ്പന്നങ്ങൾ', statBrands: 'ബ്രാൻഡുകൾ', statStates: 'സംസ്ഥാനങ്ങൾ', statFarmers: 'കർഷകർ',
      trust1Title: '100% യഥാർഥ ഉൽപ്പന്നങ്ങൾ', trust1Desc: 'CIB&RC രജിസ്ട്രേഷൻ ഉള്ള നിർമ്മാതാക്കൾ',
      trust2Title: 'കാഷ് ഓൺ ഡെലിവറി', trust2Desc: '₹10,000 വരെ COD ലഭ്യം',
      trust3Title: 'സുരക്ഷിത പേമെൻ്റ്', trust3Desc: 'UPI, കാർഡ് & നെറ്റ് ബാങ്കിംഗ്',
      trust4Title: 'വിദഗ്ധ കൃഷി സഹായം', trust4Desc: 'കൃഷി വിദഗ്ധർ തിങ്കൾ–ശനി, രാ 8–വൈ 7',
      bestSellersEyebrow: 'ബെസ്റ്റ് സെല്ലർ', bestSellersTitle: 'ഏറ്റവും കൂടുതൽ വിൽക്കുന്ന ഉൽപ്പന്നങ്ങൾ', viewAll: 'എല്ലാം കാണൂ →',
      catEyebrow: 'വിഭാഗം അനുസരിച്ച് വാങ്ങൂ', catTitle: 'നിങ്ങളുടെ വയലിന് ആവശ്യമുള്ളതെല്ലാം',
      catSub: '10 പ്രധാന വിഭാഗങ്ങളിൽ 5,000+ സർട്ടിഫൈഡ് ഉൽപ്പന്നങ്ങൾ',
      productsTitle: 'ഞങ്ങളുടെ ഉൽപ്പന്നങ്ങൾ', productsSubtitle: '5,000+ സർട്ടിഫൈഡ് കൃഷി ഉൽപ്പന്നങ്ങൾ — വിത്തുകൾ, വളങ്ങൾ, രാസവസ്തുക്കൾ, യന്ത്രങ്ങൾ, ജലസേചനം',
      categoryLabel: 'വിഭാഗം', filterAll: 'എല്ലാ ഉൽപ്പന്നങ്ങളും', filterSeeds: 'വിത്തുകൾ', filterFertilizer: 'വളങ്ങൾ',
      filterChemicals: 'രാസവസ്തുക്കൾ', filterMachinery: 'യന്ത്രങ്ങൾ', filterIrrigation: 'ജലസേചനം', filterBioInputs: 'ജൈവ ഇൻപുട്ടുകൾ',
      vendorsEyebrow: 'അംഗീകൃത പങ്കാളികൾ', vendorsTitle: 'ഞങ്ങളുടെ വെൻഡർ പങ്കാളികൾ', vendorsSubtitle: 'എല്ലാ ബ്രാൻഡുകളും KYC-സ്ഥിരീകരിക്കപ്പെട്ടതും വാർഷിക ഗുണനിലവാര ഓഡിറ്റ് ചെയ്യപ്പെട്ടതുമാണ്.',
      contactEyebrow: 'ബന്ധപ്പെടൂ', contactTitle: 'ഞങ്ങൾ സഹായിക്കാൻ ഇവിടെ ഉണ്ട്', contactSubtitle: 'ഉൽപ്പന്നങ്ങൾ, ബൾക്ക് ഓർഡറുകൾ അല്ലെങ്കിൽ ഡെലിവറിയെക്കുറിച്ച് ചോദ്യങ്ങളുണ്ടോ? ഞങ്ങളുടെ വിദഗ്ധർ തിങ്കൾ–ശനി ലഭ്യമാണ്.',
      helpEyebrow: 'പിന്തുണ കേന്ദ്രം', helpTitle: 'സഹായം & FAQ',
    },
    bn: {
      home: 'হোম', products: 'পণ্য', vendors: 'বিক্রেতা', contact: 'যোগাযোগ',
      getQuote: 'কোটেশন পান', login: 'লগইন',
      searchPlaceholder: 'বীজ, সার, ট্র্যাক্টর, কীটনাশক খুঁজুন…',
      language: 'ভাষা',
      heroPill: '12,000+ কৃষকের বিশ্বাস',
      heroTitle: 'আসল<br/>কৃষি পণ্য কিনুন<br/><em>সরাসরি</em> <strong>বিশ্বস্ত ব্র্যান্ড থেকে</strong>',
      heroSubtitle: 'মানসম্পন্ন কৃষি পণ্য সরাসরি আপনার মাঠে',
      heroCheck1: 'সার্টিফাইড পণ্য', heroCheck2: 'ক্যাশ অন ডেলিভারি',
      heroCheck3: 'বাল্ক অর্ডার পাওয়া যায়', heroCheck4: 'সারা ভারতে ডেলিভারি',
      shopProducts: 'পণ্য কিনুন', getBulkQuote: 'বাল্ক কোটেশন পান',
      statProducts: 'পণ্য', statBrands: 'ব্র্যান্ড', statStates: 'রাজ্য', statFarmers: 'কৃষক',
      trust1Title: '100% আসল পণ্য', trust1Desc: 'CIB&RC নিবন্ধিত প্রস্তুতকারক',
      trust2Title: 'ক্যাশ অন ডেলিভারি', trust2Desc: '₹10,000 পর্যন্ত COD পাওয়া যায়',
      trust3Title: 'নিরাপদ পেমেন্ট', trust3Desc: 'UPI, কার্ড ও নেট ব্যাংকিং',
      trust4Title: 'বিশেষজ্ঞ কৃষি সহায়তা', trust4Desc: 'কৃষি বিশেষজ্ঞ সোম–শনি, সকাল ৮–সন্ধ্যা ৭',
      bestSellersEyebrow: 'বেস্ট সেলার', bestSellersTitle: 'সবচেয়ে বেশি বিক্রীত পণ্য', viewAll: 'সব দেখুন →',
      catEyebrow: 'ক্যাটাগরি অনুযায়ী কিনুন', catTitle: 'আপনার মাঠের সব প্রয়োজন',
      catSub: '10টি প্রধান বিভাগে 5,000+ সার্টিফাইড পণ্য',
      productsTitle: 'আমাদের পণ্য', productsSubtitle: '5,000+ সার্টিফাইড কৃষি পণ্য — বীজ, সার, রাসায়নিক, যন্ত্রপাতি ও সেচ',
      categoryLabel: 'ক্যাটাগরি', filterAll: 'সব পণ্য', filterSeeds: 'বীজ', filterFertilizer: 'সার',
      filterChemicals: 'রাসায়নিক', filterMachinery: 'যন্ত্রপাতি', filterIrrigation: 'সেচ', filterBioInputs: 'জৈব ইনপুট',
      vendorsEyebrow: 'অনুমোদিত অংশীদার', vendorsTitle: 'আমাদের বিক্রেতা অংশীদার', vendorsSubtitle: 'সব ব্র্যান্ড KYC-যাচাইকৃত এবং বার্ষিক মানের অডিট করা।',
      contactEyebrow: 'যোগাযোগ করুন', contactTitle: 'আমরা সাহায্য করতে এখানে আছি', contactSubtitle: 'পণ্য, বাল্ক অর্ডার বা ডেলিভারি সম্পর্কে প্রশ্ন আছে? আমাদের বিশেষজ্ঞরা সোম–শনি উপলব্ধ।',
      helpEyebrow: 'সহায়তা কেন্দ্র', helpTitle: 'সাহায্য ও FAQ',
    },
    mr: {
      home: 'मुखपृष्ठ', products: 'उत्पादने', vendors: 'विक्रेते', contact: 'संपर्क',
      getQuote: 'कोटेशन मिळवा', login: 'लॉगिन',
      searchPlaceholder: 'बिया, खते, ट्रॅक्टर, कीटकनाशक शोधा…',
      language: 'भाषा',
      heroPill: '12,000+ शेतकऱ्यांनी विश्वास ठेवलेला',
      heroTitle: 'खरी<br/>कृषी उत्पादने खरेदी करा<br/><em>थेट</em> <strong>विश्वसनीय ब्रँड्सकडून</strong>',
      heroSubtitle: 'दर्जेदार कृषी उत्पादने थेट तुमच्या शेतात',
      heroCheck1: 'प्रमाणित उत्पादने', heroCheck2: 'कॅश ऑन डिलिव्हरी',
      heroCheck3: 'बल्क ऑर्डर उपलब्ध', heroCheck4: 'संपूर्ण भारतात डिलिव्हरी',
      shopProducts: 'उत्पादने खरेदी करा', getBulkQuote: 'बल्क कोटेशन मिळवा',
      statProducts: 'उत्पादने', statBrands: 'ब्रँड्स', statStates: 'राज्ये', statFarmers: 'शेतकरी',
      trust1Title: '100% खरी उत्पादने', trust1Desc: 'CIB&RC नोंदणीकृत उत्पादक',
      trust2Title: 'कॅश ऑन डिलिव्हरी', trust2Desc: '₹10,000 पर्यंत COD उपलब्ध',
      trust3Title: 'सुरक्षित पेमेंट', trust3Desc: 'UPI, कार्ड आणि नेट बँकिंग',
      trust4Title: 'तज्ञ कृषी मदत', trust4Desc: 'कृषी तज्ञ सोम–शनि, सकाळी 8–संध्याकाळी 7',
      bestSellersEyebrow: 'बेस्ट सेलर', bestSellersTitle: 'सर्वाधिक विकल्या गेलेल्या उत्पादने', viewAll: 'सर्व पाहा →',
      catEyebrow: 'श्रेणीनुसार खरेदी करा', catTitle: 'तुमच्या शेतासाठी सर्व काही',
      catSub: '10 प्रमुख श्रेणींमध्ये 5,000+ प्रमाणित उत्पादने',
      productsTitle: 'आमची उत्पादने', productsSubtitle: '5,000+ प्रमाणित कृषी उत्पादने — बिया, खते, रसायने, यंत्रसामग्री आणि सिंचन',
      categoryLabel: 'श्रेणी', filterAll: 'सर्व उत्पादने', filterSeeds: 'बिया', filterFertilizer: 'खते',
      filterChemicals: 'रसायने', filterMachinery: 'यंत्रसामग्री', filterIrrigation: 'सिंचन', filterBioInputs: 'जैव इनपुट',
      vendorsEyebrow: 'अधिकृत भागीदार', vendorsTitle: 'आमचे विक्रेते भागीदार', vendorsSubtitle: 'सर्व ब्रँड KYC-सत्यापित आणि वार्षिक गुणवत्ता ऑडिट केलेले आहेत.',
      contactEyebrow: 'संपर्क करा', contactTitle: 'आम्ही मदत करण्यास येथे आहोत', contactSubtitle: 'उत्पादने, बल्क ऑर्डर किंवा डिलिव्हरीबद्दल प्रश्न आहेत? आमचे तज्ञ सोम–शनि उपलब्ध.',
      helpEyebrow: 'मदत केंद्र', helpTitle: 'मदत आणि FAQ',
    },
    gu: {
      home: 'હોમ', products: 'ઉત્પાદનો', vendors: 'વિક્રેતાઓ', contact: 'સંપર્ક',
      getQuote: 'ક્વોટ મેળવો', login: 'લૉગિન',
      searchPlaceholder: 'બિયારણ, ખાતર, ટ્રેક્ટર, જંતુનાશક શોધો…',
      language: 'ભાષા',
      heroPill: '12,000+ ખેડૂતો દ્વારા વિશ્વાસ',
      heroTitle: 'અસલ<br/>કૃષિ ઉત્પાદનો ખરીદો<br/><em>સીધા</em> <strong>વિશ્વસનીય બ્રાન્ડ્સ પાસેથી</strong>',
      heroSubtitle: 'ગુણવત્તાવાળા ખેતી ઉત્પાદનો સીધા તમારા ખેતરે',
      heroCheck1: 'પ્રમાણિત ઉત્પાદનો', heroCheck2: 'કૅશ ઑન ડિલિવરી',
      heroCheck3: 'બલ્ક ઓર્ડર ઉપલબ્ધ', heroCheck4: 'સમગ્ર ભારતમાં ડિલિવરી',
      shopProducts: 'ઉત્પાદનો ખરીદો', getBulkQuote: 'બલ્ક ક્વોટ મેળવો',
      statProducts: 'ઉત્પાદનો', statBrands: 'બ્રાન્ડ્સ', statStates: 'રાજ્યો', statFarmers: 'ખેડૂતો',
      trust1Title: '100% અસલ ઉત્પાદનો', trust1Desc: 'CIB&RC નોંધાયેલ ઉત્પાદકો',
      trust2Title: 'કૅશ ઑન ડિલિવરી', trust2Desc: '₹10,000 સુધી COD ઉપલબ્ધ',
      trust3Title: 'સુરક્ષિત ચુકવણી', trust3Desc: 'UPI, કાર્ડ અને નેટ બૅન્કિંગ',
      trust4Title: 'નિષ્ણાત ખેતી સહાય', trust4Desc: 'કૃષિ નિષ્ણાત સોમ–શનિ, સવારે 8–સાંજે 7',
      bestSellersEyebrow: 'બેસ્ટ સેલર', bestSellersTitle: 'સૌથી વધુ વેચાતા ઉત્પાદનો', viewAll: 'બધું જુઓ →',
      catEyebrow: 'શ્રેણી પ્રમાણે ખરીદો', catTitle: 'તમારા ખેતરને જે જોઈએ તે બધું',
      catSub: '10 મુખ્ય શ્રેણીઓમાં 5,000+ પ્રમાણિત ઉત્પાદનો',
      productsTitle: 'અમારા ઉત્પાદનો', productsSubtitle: '5,000+ પ્રમાણિત ખેતી ઉત્પાદનો — બિયારણ, ખાતર, રસાયણ, યંત્ર અને સિંચાઈ',
      categoryLabel: 'શ્રેણી', filterAll: 'બધા ઉત્પાદનો', filterSeeds: 'બિયારણ', filterFertilizer: 'ખાતર',
      filterChemicals: 'રસાયણ', filterMachinery: 'યંત્ર', filterIrrigation: 'સિંચાઈ', filterBioInputs: 'જૈવ ઇનપુટ',
      vendorsEyebrow: 'અધિકૃત ભાગીદારો', vendorsTitle: 'અમારા વિક્રેતા ભાગીદારો', vendorsSubtitle: 'બધી બ્રાન્ડ KYC-ચકાસાયેલ અને વાર્ષિક ગુણવત્તા ઓડિટ.',
      contactEyebrow: 'સંપર્ક કરો', contactTitle: 'અમે મદદ કરવા અહીં છીએ', contactSubtitle: 'ઉત્પાદનો, બલ્ક ઓર્ડર અથવા ડિલિવરી વિશે પ્રશ્ન? અમારા નિષ્ણાત સોમ–શનિ ઉપલબ્ધ.',
      helpEyebrow: 'સહાય કેન્દ્ર', helpTitle: 'સહાય અને FAQ',
    },
    pa: {
      home: 'ਘਰ', products: 'ਉਤਪਾਦ', vendors: 'ਵਿਕਰੇਤਾ', contact: 'ਸੰਪਰਕ',
      getQuote: 'ਕੋਟੇਸ਼ਨ ਪ੍ਰਾਪਤ ਕਰੋ', login: 'ਲੌਗਿਨ',
      searchPlaceholder: 'ਬੀਜ, ਖਾਦ, ਟਰੈਕਟਰ, ਕੀਟਨਾਸ਼ਕ ਲੱਭੋ…',
      language: 'ਭਾਸ਼ਾ',
      heroPill: '12,000+ ਕਿਸਾਨਾਂ ਦਾ ਭਰੋਸਾ',
      heroTitle: 'ਅਸਲ<br/>ਖੇਤੀ ਉਤਪਾਦ ਖਰੀਦੋ<br/><em>ਸਿੱਧੇ</em> <strong>ਭਰੋਸੇਯੋਗ ਬ੍ਰਾਂਡਾਂ ਤੋਂ</strong>',
      heroSubtitle: 'ਵਧੀਆ ਖੇਤੀ ਉਤਪਾਦ ਸਿੱਧੇ ਤੁਹਾਡੇ ਖੇਤ ਤੱਕ',
      heroCheck1: 'ਪ੍ਰਮਾਣਿਤ ਉਤਪਾਦ', heroCheck2: 'ਕੈਸ਼ ਆਨ ਡਿਲੀਵਰੀ',
      heroCheck3: 'ਬਲਕ ਆਰਡਰ ਉਪਲਬਧ', heroCheck4: 'ਪੂਰੇ ਭਾਰਤ ਵਿੱਚ ਡਿਲੀਵਰੀ',
      shopProducts: 'ਉਤਪਾਦ ਖਰੀਦੋ', getBulkQuote: 'ਬਲਕ ਕੋਟੇਸ਼ਨ ਪ੍ਰਾਪਤ ਕਰੋ',
      statProducts: 'ਉਤਪਾਦ', statBrands: 'ਬ੍ਰਾਂਡ', statStates: 'ਰਾਜ', statFarmers: 'ਕਿਸਾਨ',
      trust1Title: '100% ਅਸਲ ਉਤਪਾਦ', trust1Desc: 'CIB&RC ਰਜਿਸਟਰਡ ਨਿਰਮਾਤਾ',
      trust2Title: 'ਕੈਸ਼ ਆਨ ਡਿਲੀਵਰੀ', trust2Desc: '₹10,000 ਤੱਕ COD ਉਪਲਬਧ',
      trust3Title: 'ਸੁਰੱਖਿਅਤ ਭੁਗਤਾਨ', trust3Desc: 'UPI, ਕਾਰਡ ਅਤੇ ਨੈੱਟ ਬੈਂਕਿੰਗ',
      trust4Title: 'ਮਾਹਿਰ ਖੇਤੀ ਸਹਾਇਤਾ', trust4Desc: 'ਖੇਤੀ ਮਾਹਿਰ ਸੋਮ–ਸ਼ਨੀ, ਸਵੇਰੇ 8–ਸ਼ਾਮ 7',
      bestSellersEyebrow: 'ਬੈਸਟ ਸੈਲਰ', bestSellersTitle: 'ਸਭ ਤੋਂ ਵੱਧ ਵਿਕਣ ਵਾਲੇ ਉਤਪਾਦ', viewAll: 'ਸਭ ਦੇਖੋ →',
      catEyebrow: 'ਸ਼੍ਰੇਣੀ ਅਨੁਸਾਰ ਖਰੀਦੋ', catTitle: 'ਤੁਹਾਡੇ ਖੇਤ ਲਈ ਹਰ ਚੀਜ਼',
      catSub: '10 ਮੁੱਖ ਸ਼੍ਰੇਣੀਆਂ ਵਿੱਚ 5,000+ ਪ੍ਰਮਾਣਿਤ ਉਤਪਾਦ',
      productsTitle: 'ਸਾਡੇ ਉਤਪਾਦ', productsSubtitle: '5,000+ ਪ੍ਰਮਾਣਿਤ ਖੇਤੀ ਉਤਪਾਦ — ਬੀਜ, ਖਾਦ, ਰਸਾਇਣ, ਮਸ਼ੀਨਰੀ ਅਤੇ ਸਿੰਚਾਈ',
      categoryLabel: 'ਸ਼੍ਰੇਣੀ', filterAll: 'ਸਭ ਉਤਪਾਦ', filterSeeds: 'ਬੀਜ', filterFertilizer: 'ਖਾਦ',
      filterChemicals: 'ਰਸਾਇਣ', filterMachinery: 'ਮਸ਼ੀਨਰੀ', filterIrrigation: 'ਸਿੰਚਾਈ', filterBioInputs: 'ਜੈਵਿਕ ਇਨਪੁੱਟ',
      vendorsEyebrow: 'ਅਧਿਕਾਰਤ ਭਾਈਵਾਲ', vendorsTitle: 'ਸਾਡੇ ਵਿਕਰੇਤਾ ਭਾਈਵਾਲ', vendorsSubtitle: 'ਸਾਰੇ ਬ੍ਰਾਂਡ KYC ਤਸਦੀਕ ਅਤੇ ਸਾਲਾਨਾ ਗੁਣਵੱਤਾ ਆਡਿਟ।',
      contactEyebrow: 'ਸੰਪਰਕ ਕਰੋ', contactTitle: 'ਅਸੀਂ ਮਦਦ ਕਰਨ ਲਈ ਇੱਥੇ ਹਾਂ', contactSubtitle: 'ਉਤਪਾਦਾਂ, ਬਲਕ ਆਰਡਰ ਜਾਂ ਡਿਲੀਵਰੀ ਬਾਰੇ ਸਵਾਲ? ਸਾਡੇ ਮਾਹਿਰ ਸੋਮ–ਸ਼ਨੀ ਉਪਲਬਧ।',
      helpEyebrow: 'ਸਹਾਇਤਾ ਕੇਂਦਰ', helpTitle: 'ਮਦਦ ਅਤੇ FAQ',
    },
    or: {
      home: 'ହୋମ', products: 'ଉତ୍ପାଦ', vendors: 'ବିକ୍ରେତା', contact: 'ଯୋଗାଯୋଗ',
      getQuote: 'ଉଦ୍ଧୃତ ପ୍ରାପ୍ତ କରନ୍ତୁ', login: 'ଲଗଇନ',
      searchPlaceholder: 'ଫସଲ ବିହନ, ସାର, ଟ୍ରାକ୍ଟର, କୀଟନାଶକ ଖୋଜ…',
      language: 'ଭାଷା',
      heroPill: '12,000+ ଚାଷୀଙ୍କ ବିଶ୍ୱାସ',
      heroTitle: 'ଆସଲ<br/>କୃଷି ଉତ୍ପାଦ କିଣନ୍ତୁ<br/><em>ସିଧାସଳଖ</em> <strong>ବିଶ୍ୱସ୍ତ ବ୍ରାଣ୍ଡଙ୍କ ଠାରୁ</strong>',
      heroSubtitle: 'ଗୁଣବତ୍ତା ଥିବା କୃଷି ଉତ୍ପାଦ ସିଧାସଳଖ ଆପଣଙ୍କ ଜମିକୁ',
      heroCheck1: 'ପ୍ରମାଣିତ ଉତ୍ପାଦ', heroCheck2: 'କ୍ୟାଶ୍ ଅନ ଡେଲିଭରି',
      heroCheck3: 'ବଲ୍କ ଅର୍ଡର ଉପଲବ୍ଧ', heroCheck4: 'ସମଗ୍ର ଭାରତରେ ଡେଲିଭରି',
      shopProducts: 'ଉତ୍ପାଦ କିଣନ୍ତୁ', getBulkQuote: 'ବଲ୍କ ଉଦ୍ଧୃତ ପ୍ରାପ୍ତ କରନ୍ତୁ',
      statProducts: 'ଉତ୍ପାଦ', statBrands: 'ବ୍ରାଣ୍ଡ', statStates: 'ରାଜ୍ୟ', statFarmers: 'ଚାଷୀ',
      trust1Title: '100% ଆସଲ ଉତ୍ପାଦ', trust1Desc: 'CIB&RC ନିବନ୍ଧିତ ଉତ୍ପାଦକ',
      trust2Title: 'କ୍ୟାଶ୍ ଅନ ଡେଲିଭରି', trust2Desc: '₹10,000 ପର୍ଯ୍ୟନ୍ତ COD ଉପଲବ୍ଧ',
      trust3Title: 'ସୁରକ୍ଷିତ ଦେୟ', trust3Desc: 'UPI, କାର୍ଡ ଏବଂ ନେଟ ବ୍ୟାଙ୍କିଙ୍ଗ',
      trust4Title: 'ବିଶେଷଜ୍ଞ କୃଷି ସହାୟତା', trust4Desc: 'କୃଷି ବିଶେଷଜ୍ଞ ସୋମ–ଶନି, ସକାଳ 8–ସନ୍ଧ୍ୟା 7',
      bestSellersEyebrow: 'ବେଷ୍ଟ ସେଲର', bestSellersTitle: 'ସର୍ବାଧିକ ବିକ୍ରୀ ଉତ୍ପାଦ', viewAll: 'ସବୁ ଦେଖନ୍ତୁ →',
      catEyebrow: 'ବର୍ଗ ଅନୁଯାୟୀ କିଣନ୍ତୁ', catTitle: 'ଆପଣଙ୍କ ଜମି ପାଇଁ ସବୁ କିଛି',
      catSub: '10 ପ୍ରମୁଖ ବର୍ଗରେ 5,000+ ପ୍ରମାଣିତ ଉତ୍ପାଦ',
      productsTitle: 'ଆମ ଉତ୍ପାଦ', productsSubtitle: '5,000+ ପ୍ରମାଣିତ କୃଷି ଉତ୍ପାଦ — ବିହନ, ସାର, ରାସାୟନିକ, ଯନ୍ତ୍ରପାତି ଏବଂ ଜଳସେଚନ',
      categoryLabel: 'ବର୍ଗ', filterAll: 'ସମସ୍ତ ଉତ୍ପାଦ', filterSeeds: 'ବିହନ', filterFertilizer: 'ସାର',
      filterChemicals: 'ରାସାୟନିକ', filterMachinery: 'ଯନ୍ତ୍ରପାତି', filterIrrigation: 'ଜଳସେଚନ', filterBioInputs: 'ଜୈବ ଇନପୁଟ',
      vendorsEyebrow: 'ଅଧିକୃତ ସଂଯୋଗ', vendorsTitle: 'ଆମ ବିକ୍ରେତା ସଂଯୋଗ', vendorsSubtitle: 'ସମସ୍ତ ବ୍ରାଣ୍ଡ KYC ଯାଞ୍ଚ ଏବଂ ବାର୍ଷିକ ଗୁଣବତ୍ତା ଅଡିଟ।',
      contactEyebrow: 'ଯୋଗାଯୋଗ କରନ୍ତୁ', contactTitle: 'ଆମେ ସାହାଯ୍ୟ କରିବାକୁ ଏଠାରେ ଅଛୁ', contactSubtitle: 'ଉତ୍ପାଦ, ବଲ୍କ ଅର୍ଡର ବା ଡେଲିଭରି ବିଷୟରେ ପ୍ରଶ୍ନ? ଆମ ବିଶେଷଜ୍ଞ ସୋମ–ଶନି ଉପଲବ୍ଧ।',
      helpEyebrow: 'ସହାୟତା କେନ୍ଦ୍ର', helpTitle: 'ସାହାଯ୍ୟ ଏବଂ FAQ',
    },
  },

  _current: 'en',
  _open: false,

  init() {
    this._buildDropdown();

    const saved = localStorage.getItem('preferred_language');
    if (saved && this.LANGUAGES.find(l => l.code === saved)) {
      this._current = saved;
      this._applyStrings(saved);
    } else {
      this._detectLocale();
    }
    this._updateBtnLabel();
    this._updateChecked();

    // Close on outside click
    document.addEventListener('click', (e) => {
      if (!document.getElementById('langSelector')?.contains(e.target)) {
        this._closeDropdown();
      }
    });
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') this._closeDropdown();
    });
  },

  _buildDropdown() {
    const dropdown = document.getElementById('langDropdown');
    if (!dropdown) return;
    dropdown.innerHTML =
      '<div class="lang-dropdown-head">Select Language</div>' +
      this.LANGUAGES.map(l => `
        <button type="button" class="lang-item" data-code="${l.code}" onclick="Lang.select('${l.code}')">
          <span class="lang-item-native">${l.native}</span>
          <span class="lang-item-en">${l.label}</span>
          <iconify-icon class="lang-item-check" icon="ph:check-bold" width="13" height="13" aria-hidden="true"></iconify-icon>
        </button>`).join('');
  },

  _detectLocale() {
    const bcp = (navigator.language || 'en').split('-')[0].toLowerCase();
    const match = this.LANGUAGES.find(l => l.bcp === bcp);
    if (match && match.code !== 'en') {
      this._showSuggestionPopup(match.code);
    }
  },

  _showSuggestionPopup(code) {
    const dismissed = localStorage.getItem('language_popup_dismissed');
    if (dismissed) {
      const ts = parseInt(dismissed, 10);
      if (Date.now() - ts < 30 * 24 * 60 * 60 * 1000) return;
    }
    const lang = this.LANGUAGES.find(l => l.code === code);
    if (!lang) return;
    const popup = document.getElementById('langPopup');
    if (!popup) return;
    const nativeEl = document.getElementById('langPopupNative');
    if (nativeEl) nativeEl.textContent = lang.native;
    document.getElementById('langPopupSwitch').onclick = () => {
      this.select(code);
      popup.classList.remove('lang-popup-show');
    };
    document.getElementById('langPopupDismiss').onclick = () => {
      localStorage.setItem('language_popup_dismissed', String(Date.now()));
      popup.classList.remove('lang-popup-show');
    };
    setTimeout(() => popup.classList.add('lang-popup-show'), 1800);
  },

  toggle() {
    this._open ? this._closeDropdown() : this._openDropdown();
  },

  _openDropdown() {
    this._open = true;
    document.getElementById('langDropdown')?.classList.add('lang-dropdown-open');
    const btn = document.getElementById('langBtn');
    if (btn) btn.setAttribute('aria-expanded', 'true');
    document.getElementById('langCaret')?.classList.add('lang-caret-open');
  },

  _closeDropdown() {
    this._open = false;
    document.getElementById('langDropdown')?.classList.remove('lang-dropdown-open');
    const btn = document.getElementById('langBtn');
    if (btn) btn.setAttribute('aria-expanded', 'false');
    document.getElementById('langCaret')?.classList.remove('lang-caret-open');
  },

  select(code) {
    this._current = code;
    localStorage.setItem('preferred_language', code);
    localStorage.setItem('language_selected_manually', 'true');
    this._applyStrings(code);
    this._updateBtnLabel();
    this._updateChecked();
    this._closeDropdown();
    // Close mobile menu too
    App.closeMenu?.();
  },

  _applyStrings(code) {
    const s = this.STRINGS[code] || this.STRINGS.en;

    const set = (id, text) => { const el = document.getElementById(id); if (el) el.textContent = text; };
    set('nl-home', s.home);
    set('nl-products', s.products);
    set('nl-vendors', s.vendors);
    set('nl-contact', s.contact);

    const quoteBtn = document.querySelector('.quote-btn');
    if (quoteBtn) quoteBtn.textContent = s.getQuote;

    const searchInput = document.getElementById('searchInput');
    if (searchInput) searchInput.placeholder = s.searchPlaceholder;

    // Login btn — preserve icon element
    const loginBtn = document.querySelector('.login-btn');
    if (loginBtn) {
      const icon = loginBtn.querySelector('iconify-icon');
      loginBtn.innerHTML = (icon ? icon.outerHTML : '') + ' ' + s.login;
    }

    // data-i18n attributes (textContent)
    document.querySelectorAll('[data-i18n]').forEach(el => {
      const key = el.getAttribute('data-i18n');
      if (s[key] !== undefined) el.textContent = s[key];
    });

    // data-i18n-html attributes (innerHTML — trusted static strings only)
    document.querySelectorAll('[data-i18n-html]').forEach(el => {
      const key = el.getAttribute('data-i18n-html');
      if (s[key] !== undefined) el.innerHTML = s[key];
    });
  },

  _updateBtnLabel() {
    const lang = this.LANGUAGES.find(l => l.code === this._current);
    const label = document.getElementById('langLabel');
    if (label && lang) label.textContent = lang.native;
  },

  _updateChecked() {
    document.querySelectorAll('.lang-item').forEach(el => {
      el.classList.toggle('lang-item-active', el.dataset.code === this._current);
    });
  },
};

const Router = {
  routes: ['home', 'products', 'vendors', 'contact', 'about', 'help', 'login'],

  go(page, pushState = true) {
    if (!this.routes.includes(page)) page = 'home';

    // Show/hide pages
    this.routes.forEach(route => {
      const el = document.getElementById(`page-${route}`);
      if (el) el.classList.toggle('active', route === page);
    });

    // Update active desktop nav link
    this.routes.forEach(route => {
      const link = document.getElementById(`nl-${route}`);
      if (link) link.classList.toggle('active', route === page);
    });

    // Update active bottom nav button
    this.routes.forEach(route => {
      const btn = document.getElementById(`bnav-${route}`);
      if (btn) btn.classList.toggle('active', route === page);
    });

    // Push to browser history so back/forward buttons work
    if (pushState) {
      const hash = page === 'home' ? '#' : '#' + page;
      // Use just the hash for cleaner and more reliable path handling on GitHub Pages
      history.pushState({ page }, '', hash);
    }

    // Close mobile menu if open
    try {
      if (typeof App !== 'undefined' && App.closeMenu) App.closeMenu();
    } catch (e) { console.warn('Router: failed to close menu', e); }

    // Scroll to top
    window.scrollTo({ top: 0, behavior: 'smooth' });

    // Page-specific init
    try {
      if (page === 'products' && typeof Products !== 'undefined') Products.render();
      if (page === 'vendors'  && typeof Vendors !== 'undefined')  Vendors.render();
    } catch (e) {
      console.error(`Router: failed to init page "${page}"`, e);
    }
  },

  // Called on DOMContentLoaded to restore page from URL hash
  init() {
    const page = location.hash.replace('#', '') || 'home';
    this.go(page, false);

    window.addEventListener('popstate', (e) => {
      const page = (e.state && e.state.page) || location.hash.replace('#', '') || 'home';
      this.go(page, false);
    });
  },
};
const Cart = {
  items: [],

  addById(id) {
    const product = (window.Products && Products.getById(id)) || Data.products.find(p => String(p.id) === String(id));
    if (product) this.add(product);
  },

  add(product) {
    const existing = this.items.find(i => String(i.id) === String(product.id));
    if (existing) {
      existing.qty = (existing.qty || 1) + 1;
    } else {
      this.items.push({ ...product, qty: 1 });
    }
    this.render();
    this.open();
    App.showToast(`${product.name} added to cart`, 'ph:shopping-cart-simple-bold');
  },

  remove(productId) {
    this.items = this.items.filter(i => String(i.id) !== String(productId));
    this.render();
  },

  updateQty(productId, qty) {
    const item = this.items.find(i => String(i.id) === String(productId));
    if (!item) return;
    item.qty = qty;
    if (item.qty <= 0) this.remove(productId);
    else this.render();
  },

  open() {
    document.getElementById('cartDrawer')?.classList.add('open');
    document.getElementById('cartOverlay')?.classList.add('show');
    // iOS Safari ignores overflow:hidden on body — use position:fixed instead
    const scrollY = window.scrollY;
    document.body.style.overflow  = 'hidden';
    document.body.style.position  = 'fixed';
    document.body.style.top       = `-${scrollY}px`;
    document.body.style.width     = '100%';
    document.body.dataset.scrollY = String(scrollY);
  },

  close() {
    document.getElementById('cartDrawer')?.classList.remove('open');
    document.getElementById('cartOverlay')?.classList.remove('show');
    const scrollY = parseInt(document.body.dataset.scrollY || '0', 10);
    document.body.style.overflow = '';
    document.body.style.position = '';
    document.body.style.top      = '';
    document.body.style.width    = '';
    window.scrollTo(0, scrollY);
  },

  _total() {
    return this.items.reduce((sum, i) => sum + i.price * (i.qty || 1), 0);
  },

  render() {
    const container = document.getElementById('cartItems');
    const badge     = document.getElementById('cartBadge');
    const totalEl   = document.getElementById('cartTotal');

    const count = this.items.reduce((s, i) => s + (i.qty || 1), 0);
    if (badge)   badge.textContent = count;
    if (totalEl) totalEl.textContent = '₹' + this._total().toLocaleString('en-IN');

    if (!container) return;
    if (!this.items.length) {
      container.innerHTML = '<div class="cart-empty">Your cart is empty</div>';
      return;
    }

    container.innerHTML = this.items.map(item => `
      <div class="cart-item">
        <div class="cart-left">
          <strong title="${item.name}">${item.name}</strong>
          <div class="cart-qty">
            <button onclick="Cart.updateQty('${item.id}', ${(item.qty || 1) - 1})">−</button>
            <span>${item.qty || 1}</span>
            <button onclick="Cart.updateQty('${item.id}', ${(item.qty || 1) + 1})">＋</button>
          </div>
        </div>
        <div class="cart-right">
          <p>₹${(item.price * (item.qty || 1)).toLocaleString('en-IN')}</p>
          <button class="btn btn-link" onclick="Cart.remove('${item.id}')">Remove</button>
        </div>
      </div>`).join('');
  },
};

﻿const Payment = {

  open() {
    if (!Cart.items.length) {
      App.showToast('Your cart is empty!', 'ph:shopping-cart-simple-bold');
      return;
    }

    const user = Api.getUser();
    if (!user) {
      App.showToast('Please login to continue checkout', 'ph:lock-bold');
      window.location.href = 'login.html';
      return;
    }

    Cart.close();
    document.getElementById('paymentModal').classList.add('open');
    this.renderSummary();

    // Pre-fill user info
    const nameInput = document.getElementById('pay-name');
    const phoneInput = document.getElementById('pay-phone');
    if (nameInput && !nameInput.value) nameInput.value = user.name || '';
    if (phoneInput && !phoneInput.value) phoneInput.value = user.phone || '';
  },

  close() {
    document.getElementById('paymentModal').classList.remove('open');
  },

  renderSummary() {
    const summary = document.getElementById('paymentSummary');
    if (!summary) return;
    const total = Cart.items.reduce((sum, i) => sum + i.price * (i.qty || 1), 0);
    const itemsHtml = Cart.items
      .map(i => `<div style="display:flex;justify-content:space-between;gap:1rem;">
        <span>${i.name} × ${i.qty}</span>
        <span>₹${(i.price * (i.qty || 1)).toLocaleString('en-IN')}</span>
      </div>`)
      .join('');
    summary.innerHTML = `
      ${itemsHtml}
      <hr />
      <p style="display:flex;justify-content:space-between;">
        <span>Total</span>
        <strong>₹${total.toLocaleString('en-IN')}</strong>
      </p>`;
  },

  async pay() {
    const btn = document.querySelector('.payment-card .btn-full');
    const originalText = btn ? btn.textContent : '';

    // Validate fields
    const name    = document.getElementById('pay-name')?.value.trim();
    const phone   = document.getElementById('pay-phone')?.value.trim();
    const address = document.getElementById('pay-address')?.value.trim();

    if (!name || !phone || !address) {
      App.showToast('Please fill in all delivery details.', 'ph:warning-fill');
      return;
    }

    if (btn) { btn.textContent = 'Processing…'; btn.disabled = true; }

    const items = Cart.items.map(i => ({ id: i.id, name: i.name, price: i.price, qty: i.qty || 1 }));
    const total = Cart.items.reduce((sum, i) => sum + i.price * (i.qty || 1), 0);

    try {
      const data = await Api.createPayment(items, total);

      if (data.order && data.order.id && window.Razorpay) {
        // Real Razorpay checkout
        const rzp = new window.Razorpay({
          key:         data.keyId,
          amount:      data.order.amount,
          currency:    'INR',
          order_id:    data.order.id,
          name:        'Uhazvumart Agro',
          description: 'Agro Inputs Order',
          prefill:     { name, contact: phone },
          handler: () => {
            App.showToast('Payment successful! Order confirmed.', 'ph:check-circle-fill');
            this._resetAfterPayment();
          },
          modal: { ondismiss: () => {
            if (btn) { btn.textContent = originalText; btn.disabled = false; }
          }},
        });
        rzp.open();
        return;
      }

      // Demo mode
      const orderId = data.orderId || ('FB-' + Date.now().toString().slice(-8));
      App.showToast(`Order placed! ID: ${orderId}`, 'ph:check-circle-fill');
      this._resetAfterPayment();

    } catch (err) {
      console.error('Payment error:', err);
      App.showToast('Payment failed. Please try again.', 'ph:x-circle-fill');
      if (btn) { btn.textContent = originalText; btn.disabled = false; }
    }
  },

  _resetAfterPayment() {
    Cart.items = [];
    Cart.render();
    this.close();
    // Clear form
    ['pay-name', 'pay-phone', 'pay-address'].forEach(id => {
      const el = document.getElementById(id);
      if (el) el.value = '';
    });
  },
};

const Search = {
  _results:   [],
  _hideTimer: null,

  init() {
    const input = document.getElementById('searchInput');
    if (input) {
      input.addEventListener('input', e => this.handle(e.target.value));
    }
  },

  showDropdown() {
    clearTimeout(this._hideTimer);
    const dd = document.getElementById('searchDropdown');
    if (dd && this._results.length) dd.style.display = 'block';
  },

  // Delay gives mousedown/touchstart on dropdown items time to fire first
  hideDropdown() {
    this._hideTimer = setTimeout(() => {
      const dd = document.getElementById('searchDropdown');
      if (dd) dd.style.display = 'none';
    }, 150);
  },

  clear() {
    clearTimeout(this._hideTimer);
    const input = document.getElementById('searchInput');
    if (input) input.value = '';
    const clear = document.getElementById('searchClear');
    if (clear) clear.style.display = 'none';
    this._results = [];
    const dd = document.getElementById('searchDropdown');
    if (dd) dd.style.display = 'none';
  },

  // Called when a dropdown result is clicked
  pick(index) {
    clearTimeout(this._hideTimer);
    const product = this._results[index];
    if (!product) return;
    this.clear();
    // Navigate to products page and show just this product
    Products._currentCategory = '';
    Router.go('products');
    // Wait for router to activate the page then render the single result
    setTimeout(() => Products.render([product]), 80);
  },

  async handle(value) {
    const term = (value || '').trim();
    const clearBtn = document.getElementById('searchClear');
    if (clearBtn) clearBtn.style.display = term ? 'block' : 'none';

    if (!term) {
      this._results = [];
      clearTimeout(this._hideTimer);
      const dd = document.getElementById('searchDropdown');
      if (dd) dd.style.display = 'none';
      return;
    }

    // Use shared API helper (falls back to local data on error)
    let list;
    try {
      const data = await Api.getProducts({ q: term, limit: 20 });
      list = data.data || data.products || [];
      list.forEach(p => {
        if (p._id && !p.id) p.id = p._id;
        if (p.vendor && typeof p.vendor === 'object') p.vendor = p.vendor.name;
        if (window.Products) Products.register(p);
      });
    } catch {
      list = Data.products.filter(p =>
        p.name.toLowerCase().includes(term.toLowerCase()) ||
        p.category.toLowerCase().includes(term.toLowerCase()) ||
        p.vendor.toLowerCase().includes(term.toLowerCase())
      );
    }

    this._results = list;

    // Populate dropdown
    const dd = document.getElementById('searchDropdown');
    if (dd) {
      if (!list.length) {
        dd.innerHTML = '<div class="search-item" style="color:var(--muted);cursor:default;">No results found</div>';
      } else {
        dd.innerHTML = list.slice(0, 6).map((p, i) => `
          <div class="search-item" onmousedown="Search.pick(${i})">
            <span>${p.name}</span>
            <small style="color:var(--muted);margin-left:auto;font-size:0.75rem;">₹${p.price.toLocaleString('en-IN')}</small>
          </div>`).join('');
      }
      dd.style.display = 'block';
    }
  },
};

const Slider = {
  current: 0,
  timer:   null,

  async init() {
    const container = document.getElementById('heroSlider');
    const dotsEl    = document.getElementById('sliderDots');
    if (!container || !dotsEl) return;

    // Render static slides immediately so the slider is never empty
    this._render(container, dotsEl, Data.sliderImages);
    this.timer = setInterval(() => this.next(), 4500);

    // Then try to load admin-managed banners from the API
    try {
      const res = await Api.getBanners('hero');
      if (res && res.data && res.data.length > 0) {
        const apiSlides = res.data.map(b => ({
          image: b.imageDesktop || b.imageMobile || Data.sliderImages[0].image,
          label: b.title    || '',
          sub:   b.subtitle || '',
          link:  b.buttonUrl || null,
        }));
        // Swap in the live banners
        clearInterval(this.timer);
        this.current = 0;
        container.querySelectorAll('.slide').forEach(el => el.remove());
        dotsEl.innerHTML = '';
        this._render(container, dotsEl, apiSlides);
        this.timer = setInterval(() => this.next(), 4500);
      }
    } catch (_) {
      // Network error or API unavailable — keep static fallback already rendered
    }
  },

  _render(container, dotsEl, slides) {
    const controls = container.querySelector('.slider-controls');
    slides.forEach((s, i) => {
      const slide = document.createElement('div');
      slide.className = 'slide' + (i === 0 ? ' active' : '');
      const caption = (s.label || s.sub)
        ? `<div class="slide-caption">
             ${s.label ? `<span class="slide-caption-label">${s.label}</span>` : ''}
             ${s.sub   ? `<span class="slide-caption-sub">${s.sub}</span>`     : ''}
           </div>`
        : '';
      const inner = `<img src="${s.image}" alt="${s.label || 'Banner'}" loading="${i === 0 ? 'eager' : 'lazy'}" />${caption}`;
      slide.innerHTML = s.link
        ? `<a href="${s.link}" style="display:contents">${inner}</a>`
        : inner;
      container.insertBefore(slide, controls);
    });

    slides.forEach((_, i) => {
      const dot = document.createElement('button');
      dot.className = 'slider-dot' + (i === 0 ? ' active' : '');
      dot.setAttribute('aria-label', `Slide ${i + 1}`);
      dot.addEventListener('click', () => this.go(i));
      dotsEl.appendChild(dot);
    });
  },

  go(index) {
    const slides = document.querySelectorAll('#heroSlider .slide');
    const dots   = document.querySelectorAll('#sliderDots .slider-dot');
    if (!slides.length) return;

    slides[this.current].classList.remove('active');
    dots[this.current].classList.remove('active');

    this.current = (index + slides.length) % slides.length;

    slides[this.current].classList.add('active');
    dots[this.current].classList.add('active');
  },

  next() { this.go(this.current + 1); },
  prev() { this.go(this.current - 1); },
};

const Vendors = {

  async fetch() {
    try {
      const data = await Api.getVendors();
      return data.data || data.vendors || Data.vendors;
    } catch {
      return Data.vendors;
    }
  },

  async render() {
    const grid = document.getElementById('vendorGrid');
    if (!grid) return;
    const vendors = await this.fetch();
    grid.innerHTML = vendors.map(v => `
      <div class="vendor-card">
        <img class="vendor-card-img" src="${v.image}" alt="${v.name}" loading="lazy" />
        <div class="vendor-card-body">
          <div class="vendor-card-head">
            <div class="vendor-name">${v.name}</div>
            ${v.verified ? '<span class="vendor-verified"><iconify-icon icon="ph:seal-check-fill" width="12" height="12"></iconify-icon> Verified</span>' : ''}
          </div>
          <div class="vendor-category">${v.category} · ${v.location}</div>
          <div class="vendor-desc">${v.desc}</div>
          <div class="vendor-stats">
            <span><iconify-icon icon="ph:star-fill" width="13" height="13" style="color:#f59e0b;vertical-align:middle"></iconify-icon> <strong>${v.rating}</strong></span>
            <span><iconify-icon icon="ph:package-bold" width="13" height="13" style="vertical-align:middle"></iconify-icon> <strong>${v.products}+</strong> products</span>
            <span><iconify-icon icon="ph:calendar-blank-bold" width="13" height="13" style="vertical-align:middle"></iconify-icon> Est. <strong>${v.since}</strong></span>
          </div>
        </div>
      </div>`).join('');
  },
};
const Products = {
  _currentCategory: '',
  _currentSubCategory: '',
  _searchQuery: '',
  _registry: new Map(),

  register(product) {
    if (!this._registry) this._registry = new Map();
    if (product) {
      const id = String(product.id || product._id || '');
      if (id) this._registry.set(id, product);
    }
  },

  getById(id) {
    if (!this._registry) this._registry = new Map();
    let p = this._registry.get(String(id));
    if (!p && window.Data && Data.products) {
      p = Data.products.find(item => String(item.id) === String(id));
      if (p) this.register(p);
    }
    return p;
  },

  // ─── Fetch from backend (with local fallback) ───────────────
  async fetch(params = {}) {
    try {
      const data = await Api.getProducts(typeof params === 'string'
        ? Object.fromEntries(new URLSearchParams(params))
        : params);
      // Backend returns { success: true, data: [...], pagination: {...} }
      const list = data.data || data.products || [];
      list.forEach(p => {
        if (p._id && !p.id) p.id = p._id;
        if (p.vendor && typeof p.vendor === 'object') p.vendor = p.vendor.name;
        this.register(p);
      });
      return list;
    } catch (err) {
      console.warn('Backend products fetch failed, using fallback data:', err);
      const list = Data.products || [];
      list.forEach(p => this.register(p));
      return list;
    }
  },

  // ─── Skeleton placeholder cards ─────────────────────────────
  _skeletonHTML(count = 6) {
    return Array.from({ length: count }, () => `
      <div class="product-skeleton">
        <div class="skeleton skel-img"></div>
        <div class="skel-body">
          <div class="skeleton skel-tag"></div>
          <div class="skeleton skel-name"></div>
          <div class="skeleton skel-desc"></div>
          <div class="skeleton skel-price"></div>
        </div>
      </div>`).join('');
  },

  // ─── Render products page ────────────────────────────────────
  async render(list) {
    const container = document.getElementById('productList');
    if (!container) return;

    // Show skeleton while data loads
    if (!list) container.innerHTML = this._skeletonHTML(8);

    let products = list;
    if (!products) {
      let raw = await this.fetch();
      
      // Filter by category
      if (this._currentCategory) {
        raw = raw.filter(p => p.category === this._currentCategory);
      }
      
      // Filter by subcategory
      if (this._currentSubCategory) {
        raw = raw.filter(p => p.subCategory === this._currentSubCategory || p.name.toLowerCase().includes(this._currentSubCategory.toLowerCase()));
      }
      
      // Filter by search query within
      if (this._searchQuery) {
        const sq = this._searchQuery.toLowerCase();
        raw = raw.filter(p => p.name.toLowerCase().includes(sq) || p.desc.toLowerCase().includes(sq) || p.vendor.toLowerCase().includes(sq));
      }
      
      products = raw;
    }

    if (!products.length) {
      container.innerHTML = '<p style="color:var(--muted);padding:2rem;grid-column:1/-1;">No products found for this selection.</p>';
    } else {
      container.innerHTML = products.map(p => App._productCardHTML(p)).join('');
    }

    // Sync active filter chip
    document.querySelectorAll('.filter-chip').forEach(chip => {
      const cat = chip.getAttribute('onclick').match(/'([^']*)'/)?.[1] || '';
      chip.classList.toggle('active', cat === this._currentCategory);
    });
    
    // Render subcategories UI
    this._renderSubCategories();
  },

  // ─── Subcategories & Search UI ─────────────────────────────
  _renderSubCategories() {
    const container = document.getElementById('subCategoryPanel');
    if (!container) return;
    
    const subCats = (this._currentCategory && Data.subCategories[this._currentCategory]) || [];
    
    let html = `
      <div class="category-search-box">
        <iconify-icon icon="ph:magnifying-glass" width="16" height="16"></iconify-icon>
        <input type="text" id="catSearchInput" placeholder="Search in ${this._currentCategory || 'all'}..." value="${this._searchQuery}" oninput="Products.searchWithin(this.value)">
      </div>
    `;
    
    if (subCats.length > 0) {
      html += `<div class="sub-category-pills">
        <button class="sub-cat-pill ${!this._currentSubCategory ? 'active' : ''}" onclick="Products.filterBySubCategory('')">All</button>
        ${subCats.map(sc => `<button class="sub-cat-pill ${this._currentSubCategory === sc ? 'active' : ''}" onclick="Products.filterBySubCategory('${sc}')">${sc}</button>`).join('')}
      </div>`;
    }
    
    container.innerHTML = html;
  },

  // ─── Actions ──────────────────────────────────────
  filterByCategory(category) {
    this._currentCategory = category;
    this._currentSubCategory = ''; // Reset subcategory on main category change
    // We intentionally keep _searchQuery active across categories? Usually better to reset.
    this._searchQuery = '';
    const searchInput = document.getElementById('catSearchInput');
    if (searchInput) searchInput.value = '';
    
    Router.go('products');
  },
  
  filterBySubCategory(subCategory) {
    this._currentSubCategory = subCategory;
    this.render();
  },
  
  searchWithin(query) {
    this._searchQuery = query;
    this.render();
  },

  // ─── Featured grid (home page) ───────────────────────────────
  async fetchFeatured(limit = 8) {
    const grid = document.getElementById('featuredGrid');
    if (!grid) return;

    // Show skeleton immediately
    grid.innerHTML = this._skeletonHTML(limit);

    const list = await this.fetch({ limit });
    const badged  = list.filter(p => p.badge === 'Best Seller');
    const rest    = list.filter(p => p.badge !== 'Best Seller');
    const display = [...badged, ...rest].slice(0, limit);
    grid.innerHTML = display.map(p => App._productCardHTML(p)).join('');
  },

  // ─── Category sections (home page) ──────────────────────────
  async fetchCategorySection(gridId, category, limit = 5) {
    const grid = document.getElementById(gridId);
    if (!grid) return;
    grid.innerHTML = this._skeletonHTML(limit);
    const all = await this.fetch({ limit: 100 });
    const cat = category.toLowerCase();
    const filtered = all.filter(p => (p.category || '').toLowerCase() === cat);
    const display = filtered.slice(0, limit);
    if (display.length) {
      grid.innerHTML = display.map(p => App._productCardHTML(p)).join('');
    } else {
      const section = grid.closest('section');
      if (section) section.style.display = 'none';
    }
  },
};

/* ═══════════════════════════════════════════
   APP — global utilities + page initialisation
═══════════════════════════════════════════ */

const App = {

  // ─── Toast ─────────────────────────────────────────────────
  _toastTimer: null,
  showToast(message, icon = 'ph:check-circle-fill') {
    const el = document.getElementById('toast');
    if (!el) return;
    const iconMarkup = icon
      ? icon.startsWith('images/icons/')
        ? `<img src="${icon}" alt="" class="toast-icon" />`
        : icon.includes(':')
          ? `<iconify-icon icon="${icon}" width="20" height="20" class="toast-icon"></iconify-icon>`
          : `<span class="toast-icon">${icon}</span>`
      : '';
    el.innerHTML = `${iconMarkup}<span>${message}</span>`;
    el.classList.add('show');
    clearTimeout(this._toastTimer);
    this._toastTimer = setTimeout(() => el.classList.remove('show'), 3000);
  },

  // ─── Mobile menu ────────────────────────────────────────────
  toggleMenu() {
    const nav = document.getElementById('mainNav');
    const ham = document.getElementById('hamburger');
    if (!nav || !ham) return;
    const isOpen = nav.classList.contains('menu-open');
    nav.classList.toggle('menu-open', !isOpen);
    ham.classList.toggle('open', !isOpen);
    ham.setAttribute('aria-expanded', String(!isOpen));
  },

  closeMenu() {
    const nav = document.getElementById('mainNav');
    const ham = document.getElementById('hamburger');
    if (!nav || !ham) return;
    nav.classList.remove('menu-open');
    ham.classList.remove('open');
    ham.setAttribute('aria-expanded', 'false');
  },

  // ─── Contact form submit ────────────────────────────────────
  submitContact(btn) {
    const original = btn.textContent;
    btn.textContent = 'Sending…';
    btn.disabled = true;
    setTimeout(() => {
      btn.textContent = original;
      btn.disabled = false;
      App.showToast('Message sent! We\'ll reply within 24 hours.', 'ph:envelope-fill');
    }, 1200);
  },

  // ─── Authentication ────────────────────────────────────────
  _authMode: 'login',

  setAuthMode(mode) {
    this._authMode = mode;
    const isLogin = mode === 'login';

    document.getElementById('tab-login').classList.toggle('active', isLogin);
    document.getElementById('tab-register').classList.toggle('active', !isLogin);

    document.getElementById('authTitle').textContent = isLogin ? 'Welcome Back' : 'Create Account';
    document.getElementById('authDesc').textContent = isLogin
      ? 'Login to manage your agro orders and farm data.'
      : 'Join 12,000+ farmers across India today.';

    document.getElementById('field-name').style.display = isLogin ? 'none' : 'block';
    document.getElementById('field-phone').style.display = isLogin ? 'none' : 'block';
    document.getElementById('authSubmit').textContent = isLogin ? 'Login to Uhazvumart' : 'Create My Account';
  },

  async handleAuthSubmit() {
    const email = document.getElementById('auth-email').value;
    const password = document.getElementById('auth-password').value;
    const submitBtn = document.getElementById('authSubmit');

    if (!email || !password) return this.showToast('Please fill all required fields', 'ph:warning-fill');

    const original = submitBtn.textContent;
    submitBtn.textContent = 'Processing…';
    submitBtn.disabled = true;

    try {
      let data;
      if (this._authMode === 'login') {
        data = await Api.login(email, password);
      } else {
        const name = document.getElementById('auth-name').value;
        const phone = document.getElementById('auth-phone').value;
        if (!name) throw new Error('Full name is required');
        data = await Api.register(name, email, phone, password);
      }

      this.showToast(data.message || 'Success!', 'ph:check-circle-fill');
      this.updateAuthUI();
      Router.go('home');
    } catch (err) {
      this.showToast(err.message || 'Authentication failed', 'ph:x-circle-fill');
    } finally {
      submitBtn.textContent = original;
      submitBtn.disabled = false;
    }
  },

  updateAuthUI() {
    const user = Api.getUser();
    const container = document.getElementById('nav-user-info');
    const bnavAccount = document.getElementById('bnav-login');

    if (!container) return;

    if (user) {
      container.innerHTML = `
        <div class="user-profile-nav">
          <div class="user-avatar">${user.name.charAt(0)}</div>
          <span class="user-name">${user.name}</span>
          <button onclick="App.logout()" class="logout-btn" title="Logout">
            <iconify-icon icon="ph:sign-out-bold" width="18" height="18"></iconify-icon>
          </button>
        </div>`;
      if (bnavAccount) {
        bnavAccount.querySelector('iconify-icon').setAttribute('icon', 'ph:user-circle-fill');
        bnavAccount.querySelector('span').textContent = user.name.split(' ')[0];
      }
    } else {
      container.innerHTML = `
        <a href="login.html" class="login-btn">
          <iconify-icon icon="ph:user-bold" width="18" height="18"></iconify-icon>
          Login
        </a>`;
      if (bnavAccount) {
        bnavAccount.querySelector('iconify-icon').setAttribute('icon', 'ph:user-bold');
        bnavAccount.querySelector('span').textContent = 'Account';
      }
    }
  },

  goAccount() {
    window.location.href = 'login.html';
  },

  logout() {
    Api.logout();
    this.updateAuthUI();
    this.showToast('Logged out successfully', 'ph:sign-out-bold');
    Router.go('home');
  },

  // ─── Ticker marquee ─────────────────────────────────────────
  initTicker() {
    const track = document.getElementById('tickerTrack');
    if (!track) return;
    // Duplicate items for seamless infinite scroll
    const items = [...Data.ticker, ...Data.ticker];
    track.innerHTML = items
      .map(t => {
        const iconMarkup = t.icon && t.icon.startsWith('images/icons/')
          ? `<img class="ticker-icon" src="${t.icon}" alt="" loading="lazy" />`
          : t.icon && t.icon.includes(':')
            ? `<iconify-icon icon="${t.icon}" width="15" height="15" class="ticker-icon"></iconify-icon>`
            : `<span class="ticker-icon">${t.icon || ''}</span>`;
        return `<span class="ticker-item">${iconMarkup}${t.text}</span>`;
      })
      .join('');
  },

  // ─── Promo countdown timer ──────────────────────────────────
  initPromoTimer() {
    const el = document.getElementById('promoTimer');
    if (!el) return;
    // Target: last day of current month
    const now = new Date();
    const target = new Date(now.getFullYear(), now.getMonth() + 1, 0, 23, 59, 59);
    const render = () => {
      const diff = target - new Date();
      if (diff <= 0) { el.innerHTML = ''; return; }
      const d = Math.floor(diff / 86400000);
      const h = Math.floor((diff % 86400000) / 3600000);
      const m = Math.floor((diff % 3600000) / 60000);
      const s = Math.floor((diff % 60000) / 1000);
      const pad = n => String(n).padStart(2, '0');
      el.innerHTML = `
        <div class="promo-timer">
          <div class="timer-block"><span class="timer-val">${pad(d)}</span><span class="timer-label">Days</span></div>
          <span class="timer-sep">:</span>
          <div class="timer-block"><span class="timer-val">${pad(h)}</span><span class="timer-label">Hrs</span></div>
          <span class="timer-sep">:</span>
          <div class="timer-block"><span class="timer-val">${pad(m)}</span><span class="timer-label">Mins</span></div>
          <span class="timer-sep">:</span>
          <div class="timer-block"><span class="timer-val">${pad(s)}</span><span class="timer-label">Secs</span></div>
        </div>`;
    };
    render();
    setInterval(render, 1000);
  },

  // ─── Category carousel (home page) ─────────────────────────
  initCategories() {
    const track = document.getElementById('catGrid');
    if (!track) return;
    track.innerHTML = Data.categories
      .map(cat => {
        const catIcon = cat.icon.includes(':')
          ? `<iconify-icon icon="${cat.icon}" width="30" height="30" class="cat-icon"></iconify-icon>`
          : `<span class="cat-icon">${cat.icon}</span>`;
        return `
        <div class="cat-card" onclick="Products.filterByCategory('${cat.id}')">
          <img class="cat-img" src="${cat.image}" alt="${cat.label}" loading="lazy" />
          <div class="cat-body">
            ${catIcon}
            <span class="cat-label">${cat.label}</span>
            <span class="cat-count">${cat.count.toLocaleString()} products</span>
          </div>
        </div>`;
      })
      .join('');

    // Auto-advance every 3 s, pause on hover
    let autoTimer = setInterval(() => this.catScroll(1), 3000);
    track.addEventListener('mouseenter', () => clearInterval(autoTimer));
    track.addEventListener('mouseleave', () => {
      autoTimer = setInterval(() => this.catScroll(1), 3000);
    });
  },

  // Scroll the category carousel by 2 cards at a time
  catScroll(dir) {
    const track = document.getElementById('catGrid');
    if (!track) return;
    const cardWidth = 185 + 20; // card + gap (approx)
    const maxScroll = track.scrollWidth - track.clientWidth;
    const next = track.scrollLeft + dir * cardWidth * 2;
    // Wrap around
    if (next > maxScroll + 10) {
      track.scrollTo({ left: 0, behavior: 'smooth' });
    } else if (next < -10) {
      track.scrollTo({ left: maxScroll, behavior: 'smooth' });
    } else {
      track.scrollBy({ left: dir * cardWidth * 2, behavior: 'smooth' });
    }
  },

  // ─── Vendor carousel strip (home page) ─────────────────────
  initVendorCarousel() {
    const el = document.getElementById('vendorCarousel');
    if (!el) return;
    el.innerHTML = Data.vendors
      .map(v => `
        <div class="vendor-logo-card" onclick="Router.go('vendors')">
          ${v.name}
          <span class="vendor-logo-tag">${v.category}</span>
        </div>`)
      .join('');
  },

  // ─── Crop section (home page) ───────────────────────────────
  initCropSection() {
    const grid = document.getElementById('cropGrid');
    if (!grid) return;
    grid.innerHTML = Data.crops.map(crop => `
      <div class="crop-card" onclick="Products.filterByCategory('${crop.category}');App.showToast('${crop.label}','ph:leaf-fill')">
        <iconify-icon icon="${crop.icon}" width="40" height="40" class="crop-icon"></iconify-icon>
        <span class="crop-label">${crop.label}</span>
        <span class="crop-link">Shop →</span>
      </div>`).join('');
  },

  // ─── Featured grid (home page) — API-backed ─────────────────
  initFeatured() {
    return Products.fetchFeatured(12);
  },

  // ─── Category sections (home page) ──────────────────────────
  initCategorySections() {
    Products.fetchCategorySection('homeSeedsGrid', 'seeds', 5);
    Products.fetchCategorySection('homeFertGrid',  'fertilizer', 5);
    Products.fetchCategorySection('homeChemGrid',  'chemical', 5);
    Products.fetchCategorySection('homeIrrGrid',   'irrigation', 4);
    Products.fetchCategorySection('homeMachGrid',  'machinery', 5);
  },

  // ─── FAQ accordion (help page) ──────────────────────────────
  initFAQ() {
    const list = document.getElementById('faqList');
    if (!list) return;
    list.innerHTML = Data.faqs.map((faq, i) => `
      <div class="faq-item" id="faq-${i}">
        <div class="faq-question" onclick="App.toggleFAQ(${i})">
          <span>${faq.question}</span>
          <iconify-icon icon="ph:caret-down-bold" width="20" height="20" class="faq-chevron"></iconify-icon>
        </div>
        <div class="faq-answer">${faq.answer}</div>
      </div>`).join('');
  },

  toggleFAQ(index) {
    const item = document.getElementById('faq-' + index);
    if (!item) return;
    const isOpen = item.classList.contains('open');
    // Close all
    document.querySelectorAll('.faq-item.open').forEach(el => el.classList.remove('open'));
    if (!isOpen) item.classList.add('open');
  },

  // ─── Shared product card HTML builder ───────────────────────
  _productCardHTML(p) {
    const badgeMap = {
      'Best Seller': 'badge-best',
      'New':         'badge-new',
      'Certified':   'badge-cert',
      'Organic':     'badge-org',
      'Top Pick':    'badge-top',
      'CIB&RC':      'badge-cert',
    };
    const badgeClass = badgeMap[p.badge] || '';
    const badgeHTML = p.badge
      ? `<span class="badge ${badgeClass}">${p.badge}</span>`
      : '';
    const stars = Array.from({ length: Math.floor(p.rating) }, () =>
      '<iconify-icon icon="ph:star-fill" width="12" height="12" style="color:#FFB300"></iconify-icon>'
    ).join('');
    const shortDesc = p.desc ? p.desc.slice(0, 60) + (p.desc.length > 60 ? '…' : '') : '';

    const discountPct = p.oldPrice ? Math.round((1 - p.price / p.oldPrice) * 100) : 0;
    const priceHTML = p.oldPrice
      ? `<div class="price-stack">
           <div class="price-top">
             <span class="product-price">₹${p.price.toLocaleString('en-IN')}</span>
             <span class="price-discount">${discountPct}% OFF</span>
           </div>
           <span class="product-old-price">₹${p.oldPrice.toLocaleString('en-IN')}</span>
         </div>`
      : `<span class="product-price">₹${p.price.toLocaleString('en-IN')}</span>`;

    return `
      <div class="product-card">
        <div class="product-img-wrap">
          <img class="product-img" src="${p.image}" alt="${p.name}" loading="lazy" />
          ${badgeHTML}
          <button class="card-wishlist" onclick="App.showToast('Added to wishlist','ph:heart-fill');event.stopPropagation();" aria-label="Wishlist">
            <iconify-icon icon="ph:heart-bold" width="16" height="16"></iconify-icon>
          </button>
          <button class="card-add-overlay" onclick="Cart.addById('${p.id}')">
            <iconify-icon icon="ph:shopping-cart-simple-bold" width="16" height="16"></iconify-icon>
            Add to Cart
          </button>
        </div>
        <div class="product-body">
          <div class="product-vendor-tag">${p.vendor}</div>
          <div class="product-name">${p.name}</div>
          <div class="product-meta">
            <span class="rating-star">${stars}</span>
            <span class="rating-val">${p.rating}</span>
          </div>
          <div class="product-desc">${shortDesc}</div>
          <div class="price-row">
            ${priceHTML}
            <button class="add-btn" onclick="Cart.addById('${p.id}')">+ Cart</button>
          </div>
        </div>
      </div>`;
  },
};

/* ─── Boot ─────────────────────────────────────────────────── */
document.addEventListener('DOMContentLoaded', () => {
  Slider.init();
  Search.init();
  Lang.init();
  App.initTicker();
  App.initPromoTimer();
  App.initCategories();
  App.initVendorCarousel();
  App.initCropSection();
  App.initCategorySections();
  App.initFAQ();
  App.updateAuthUI();
  Router.init();
  Cart.render();
});